fix(viewer): prevent context menu submenu from being cropped at top

Increase safe top margin (12px to 48px) and near-top threshold (80px to 100px)
so Copy & Export and other submenus stay below terminal tab bar and panel
headers.

# Options Panel

1. Add the user settings reset command to the OPTIONS panel

2. The EXPORT action is in the options panel. this is misplaced as users would expect that it exports OPTIONS. Where to logically move the export button?

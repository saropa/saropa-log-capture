# Changelog

All notable changes to Saropa Log Capture will be documented in this file.

**VS Code Marketplace** - [marketplace.visualstudio.com / saropa.saropa-log-capture](https://marketplace.visualstudio.com/items?itemName=saropa.saropa-log-capture)

**Open VSX Registry** - [open-vsx.org / extension / saropa / saropa-log-capture](https://open-vsx.org/extension/saropa/saropa-log-capture)

**GitHub Source Code** - [github.com / saropa / saropa-log-capture](https://github.com/saropa/saropa-log-capture)

For older versions (7.11.2 and prior), see [CHANGELOG_ARCHIVE.md](./CHANGELOG_ARCHIVE.md).

<!-- MAINTENANCE NOTES -- IMPORTANT --

    The format is based on [Keep a Changelog](https://keepachangelog.com/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    **Overview** — Each release (and [Unreleased]) opens with one plain-language line for humans—user-facing only, casual wording—then end it with: [log](https://github.com/saropa/saropa-log-capture/blob/vX.Y.Z/CHANGELOG.md)
    substituting X.Y.Z.

    **Tagged changelog** — Published versions use git tag **`vx.y.z`**; compare to [current `main`](https://github.com/saropa/saropa-log-capture/blob/main/CHANGELOG.md).

    **Published version**: See field "version": "x.y.z" in [package.json](./package.json)

    NOTE: try to keep this file to approx 500 lines
    
cspell:disable
-->

---

## [Unreleased]

### Added

- **Per-issue trend mini-charts in the list:** Each crash issue now shows a small sparkline of its daily event count over the selected period, so you can spot a spiking issue versus a flat one without opening it. Fetched once when the list loads.
- **Archive crash issues locally:** Each issue row now has an archive button to hide issues you've already triaged. A **Show archived** toggle in the filter bar reveals them again, each with an unarchive button. Archiving is local to your workspace (the Play Reporting data is read-only, so this doesn't close the issue in Firebase) and archived issues are skipped by the background new-issue alerts.
- **Background alerts for new and returned crashes:** Turn on **Notify on new Crashlytics issues** (off by default) and the extension checks for new or returned crash issues in the background — no need to open the panel — and shows a notification plus a status-bar badge when one appears, with a **View** action that opens the sidebar. Each issue alerts once; an issue that disappears and comes back alerts again.
- **"Regressed" tag on crashes that came back:** Issues that disappeared in an earlier refresh and have since reappeared now carry a "Regressed" badge. This is tracked locally across refreshes (a short history of past scans is now kept under `.saropa/cache/crashlytics/`), since the Play Reporting API doesn't expose issue state.
- **"Repetitive" tag on recurring crashes:** Issues seen across more than one app version now carry a "Repetitive" badge in the list, so a crash that has survived a release stands out from a one-off. Derived locally from the version range (the Play Reporting API has no such signal).
- **Sort the Crashlytics issue list by events or users:** A new sort dropdown in the issue-list filter bar lets you order issues by most events (the default, matching the server order) or most affected users — useful for telling "noisy for one user" apart from "hitting many people." Sorting is instant and client-side.
- **Crash-free sessions headline on the Vitals panel:** A prominent "Crash-free sessions" figure (100 − crash rate) now sits at the top of the Vitals panel — the single daily-health number the Firebase console leads with — alongside a period change arrow (↑ green when improving). The existing Crash Rate and ANR Rate cards stay below it. (Labeled "sessions": crash-free *users* uses a different denominator and is a separate addition.)
- **Trend sparkline on the Vitals panel:** The Crash Rate and ANR Rate cards now show a small line chart of the last several days beneath the number, so you can see at a glance whether a rate is climbing, falling, or steady — not just today's value. The data was already being fetched and thrown away; nothing new is requested from Google Play.

### Changed

- **The "Open in Firebase" arrow now deep-links to the exact issue:** Clicking ↗ beside a crash's name previously could only open the whole issues list. It now opens that specific issue in the Firebase Crashlytics console (the issue ids match between the two), so you land on the crash you were looking at instead of hunting for it.

### Fixed

- **"Open in Firebase" arrow now reaches the Crashlytics console:** The ↗ link beside a crash issue's name (and the panel's "Open Firebase Console" link) built its URL from the Firebase *app id* (`1:NNN:android:HEX`), which the console rejects with "This app does not exist or you do not have permission to view it." The link now uses the platform-prefixed package name (`android:com.example.app`) the console actually expects, so it opens the right project.

## [8.0.5]

The Session Flow Map's diagram now centers, scrolls instead of cropping when you zoom in, opens in its own pop-out panel, and shows a full detail card when you double-click a node — plus return-to-caller navigation draws a distinct back arrow. Flutter exception blocks now fold to their title (collapsed by default) and the left accent rail on error blocks is gone, keeping the log columns aligned. [log](https://github.com/saropa/saropa-log-capture/blob/v8.0.5/CHANGELOG.md)

### Added

- **Copy and collapse ASCII art blocks:** Right-click anywhere inside a grouped ASCII art / box-drawing banner (e.g. the Drift debug-server box) to get a dedicated **Copy ASCII art** item that grabs every line of the block in one gesture — the same one-gesture treatment errors already have with **Copy Error**. Each art block also now carries a ▾ disclosure chevron in its top-right corner: click it to fold the block down to its top row (showing "▸ N") and click again to restore it. Blocks stay fully expanded by default.
- **Double-click a flow node for full detail:** Double-clicking any node in the Session Flow Map diagram opens a detail card listing everything known about that surface — type, walked/source-resolved state, visit count, time on screen, first-entered and last-seen clocks, the source `file:line`, the originating log line, the in-screen action breakdown, and any attached issues. The source and log line are clickable (open in editor / reveal in log). Close with Esc, the ✕, or a click outside (plan 056, S3).
- **Pop the flow chart into its own panel:** A new ⤢ button in the diagram's overlay toolbar opens the flow chart full-bleed in a panel beside the report, so a tall or wide chart gets the whole panel to itself. It stays in sync when you refresh the report (plan 056, S3).
- **Return-to-caller back arrows:** When a session closes a screen (or several) and navigates back to an earlier screen, the diagram now draws a distinct dashed-blue back arrow (with ×N when the return repeats) from the closed surface to the caller, recovered from a navigation stack so returns of any depth are detected — not just an immediate A→B→A pop. The Markdown/Mermaid export marks the same edge with a ↩ (plan 056).
- **Honest notice when your editor's language is largely untranslated:** When the extension runs in a display language whose menus and settings are still mostly English (currently every shipped non-English locale, at 22–37% translated), it now shows a one-time notice naming the language and its coverage percent, and reassures that the log viewer itself is fully localized. Shown once per language, never repeated. Backed by a new translation audit (`npm run generate:nls-coverage`) that measures how many manifest strings are actually translated per locale — not just that the keys exist — so the percent can never overstate reality.

### Changed

- **Flow map diagram centers and scrolls instead of cropping when zoomed:** The diagram now lives in a scroll container and zoom scales the SVG's real size, so zooming in grows scrollbars rather than clipping the chart at the panel edge, and the chart is centered (and fit to view) when it is smaller than the viewport. **Center the fault** no longer zooms the whole chart away — it scrolls to the crash node at a readable zoom and flashes it (plan 056, S3).
- **Flow edge time labels moved off the arrow:** The "time on screen" (dwell) label on each transition is now parked to the right of the arrow shaft instead of painted on top of it, where it was unreadable (plan 056).
- **Flow map column splitter no longer locks to content width:** Dragging the divider between the Session Flow Map's diagram and detail columns previously stopped at the intrinsic width of the widest cell (a ~260px diagram floor and ~280px detail reserve). It now honors any ratio down to a 20px sliver on each side — the columns crop overflow (`overflow: hidden`, wrap-then-clip) rather than refusing to shrink, and the 20px floor keeps the gripper grabbable so you can always drag back (plan 056).
- **Flutter exception blocks now collapse to their title, collapsed by default:** A Flutter framework exception (the `════ Exception caught by … ════` block, including the long RenderFlex/constraints dump) now folds to just its header line with a ▶ disclosure triangle and a hidden-line count (e.g. "47 lines") — click the header to expand or re-collapse, and Collapse All / Expand All include them. So a 40+ line layout-error dump no longer floods the log; you see one red-tinted title that tells you how much it hides and open it on demand.
- **Removed the left accent rail on error/exception blocks:** Flutter exception blocks and error stack traces no longer draw a continuous colored border down their left edge. The rail pushed those rows out of column with every other line (breaking the new gutter grid) and just duplicated the per-row severity dots that already mark the block as an error. The block now reads via a faint background tint instead, staying perfectly column-aligned.
- **Jump-to-line highlight no longer nudges the target row:** The accent stripe on the line you jump or peek to is now painted as an inset shadow instead of a real left border, so the highlighted row's line number and text stay aligned with every other row instead of shifting 3px right (same fix already used by AI activity rows).

### Fixed

- **Stale viewer render tests:** Two webview-render assertions still expected the pre-grid output and were failing — the stack-frame test expected the dropped `line-deco-spacer-only` alignment spacer (Plan 055 frames now nest in the message track with no decoration cell), and the continuation-badge test expected `contBadge` to be the literal first token of `msgInner` before the new Flutter-exception `bannerChevron` was prepended ahead of it. Both now assert the current shipped behavior.

## [8.0.4]

When "App Only" is off but a category isn't whitelisted, the output channel now tells you exactly which Debug Console lines were skipped and how to capture them. [log](https://github.com/saropa/saropa-log-capture/blob/v8.0.4/CHANGELOG.md)

### Added

- **Compare 3 logs at once:** New command **Saropa Log Capture: Compare 3 Logs** picks a baseline plus two runs and opens a Markdown report that leads with a triage summary — **new errors in B/C vs the baseline** and **errors that look resolved** — followed by which lines are unique to each run, shared by a pair, or present in all three. Lines are matched by content (timestamps ignored) so the same event lines up across runs (plan 031, MVP). The existing 2-log comparison is unchanged.
- **Optional cross-workspace noise patterns (off by default):** Turn on **Share noise patterns across workspaces** (`saropaLogCapture.learning.globalAggregates`) to let a high-confidence framework-noise filter you accept in one project start suppressing the same noise in your other projects. Only the pattern text and category are stored on your machine — never paths, project names, or usernames (enforced by a deny-list) — and the new **Clear Cross-Workspace Noise Patterns** command wipes them at any time (plan 053 Workstream D).
- **"View Related Requests" popover:** Right-click any log line → **View Related Requests** to open a dedicated popover listing the HTTP requests correlated to that line (by request ID when your `integrations.http.requestIdPattern` matches, otherwise by the context time window), each showing method, URL, status, and duration, with per-row and Copy All actions. Complements the existing inline HTTP section in the integration-context popover (plan 010).
- **Dropped-category diagnostics:** When `captureAll` is off and a DAP output category is not in `saropaLogCapture.categories`, the first dropped line of each category now logs an actionable hint to the **Saropa Log Capture** output channel (category name + how to capture it), instead of vanishing silently. Documents the DAP-only capture boundary under README → Known Limitations (plan 102).
- **Localized the new compare-3-logs, related-requests, flow-map, signal-sort, and cross-workspace-clear strings** — the 24 new user-facing strings from this release's features (the 3-way comparison titles and pickers, the "View Related Requests" popover labels, the flow-map zoom / reset / center-fault controls, the signal sort toggle, the cross-workspace noise clear prompt, and the SQL-history pager) are now hand-translated into all 10 non-English locales (de, es, fr, it, ja, ko, pt-br, ru, zh-cn, zh-tw), bringing every bundle back to 1590/1590 keys. Provenance is recorded as `manual` (high-quality tier).

### Changed

- **Recursive crash stacks collapse repeated frames:** In the crash-issue detail's smart stack view, a run of identical consecutive frames (e.g. a stack-overflow from runaway recursion) now renders as a single row with a **↻ ×N** badge instead of N identical lines — so a 4,000-frame self-recursion reads at a glance. The frame count in the header still reflects the true total (plan 054, Stage 5b).
- **Crash "Other Threads" collapses identical threads:** The crash-issue detail's Other Threads panel now groups every non-crash thread by its stack and shows one representative row per distinct stack, carrying a **×N** badge (plus the collapsed thread names) when several threads share it — so a 60-thread dump of identical binder/pool/GC waiters reads as a handful of distinct stacks instead of a wall. The header shows the true total and the unique count; groups beyond the first eight are summarized, not dropped, and native-only threads now appear too (top frames), not just threads with app code (plan 054, Stage 5b).
- **Sort "Signals in this log" by time:** A new toggle under the time-window chips in the Signals panel flips the in-log signal list between **By severity** (the default) and **By time** (chronological), so you can reconstruct the order signals actually fired. Signals without a timestamp sink to the end; the severity default is unchanged (plan 052, Fu5).
- **Noise-filter suggestions now learn from your accept/reject history:** When the learner proposes filter suggestions, a pattern you previously accepted is reinforced and one you rejected is damped — and rejecting a pattern also damps its near-twins, so the same framework noise stops re-surfacing under slightly different text. Coefficients are bounded so feedback can't fully erase or runaway-inflate an extracted pattern (plan 053 Workstream C).
- **AI activity rows use the overlap-proof column grid:** AI breadcrumb lines (prompts, edits, bash, reads) now render on the same per-row CSS grid as regular log lines, so their decoration columns clip to their own tracks and can never paint over the message text (plan 055 Phase 2). No visible change in the common case; removes a latent overlap path.
- **Stack traces use the overlap-proof column grid:** Multi-frame stack-trace headers and their frames now render on the same per-row CSS grid as regular log lines — the header's line number/chevron clip to the gutter and its text lives in a dedicated message cell, and each frame nests under that message column structurally instead of via a hand-tuned indent. A long stack header can no longer paint over the gutter, and frames stay aligned under their header (plan 055 Phase 2). No visible change in the common case; removes the last hanging-indent path for log rows.
- **Flow map diagram is now an interactive lens:** The Session Flow Map panel's diagram pans (drag the background) and zooms (mouse wheel or the new +/− buttons), with a fit-to-view reset and — when the session faulted — a one-click **💥** button that centers and flashes the crash node. Node clicks (highlight row + jump to log) and the horizontal-scroll fallback are unchanged (plan 056 Stage 2).
- **SQL query history panel scales to large workspaces:** The Database panel's query-history table now renders every row as before up to 2,000 distinct fingerprints; beyond that it windows the table and shows a "Show more" pager, avoiding the jank of building thousands of rows in one pass. No change for normal-sized logs (DB_18b item 1c).

## [8.0.3]

The viewer now refreshes itself when something outside the editor rewrites, truncates, or deletes the log you're watching, and you can diff your current log against the version captured at an earlier Git commit. [log](https://github.com/saropa/saropa-log-capture/blob/v8.0.3/CHANGELOG.md)

### Added

- **The open log now reloads when an external process rewrites, truncates, or deletes it** (plan 039b) — the live-tail watcher already appended new lines as a log grew, but a file that was truncated or rewritten in place left stale rows on screen, and a deleted file showed content that no longer existed. The watcher now classifies each change: growth appends (as before), a shrink/rewrite triggers a full reload (clear + re-read + re-tail), a recreate (tools that unlink-then-write) reloads, and a delete keeps the last loaded snapshot and warns. Two settings govern it: `saropaLogCapture.reloadOnExternalChange` (default on — the escape hatch) and `saropaLogCapture.reloadPromptOnExternalChange` (default off — ask "File changed on disk. Reload?" instead of reloading silently). The grow/shrink/equal decision is a pure, unit-tested function. ([tail-change-classify.ts](src/ui/provider/tail-change-classify.ts), [log-viewer-provider-load.ts](src/ui/provider/log-viewer-provider-load.ts), [log-viewer-provider-tailing.ts](src/ui/provider/log-viewer-provider-tailing.ts), [log-viewer-provider-external-reload.ts](src/ui/provider/log-viewer-provider-external-reload.ts))
- **Compare the current log to a Git commit baseline** (plan 035) — two new commands, **Compare Log to Previous Commit** (`HEAD~1`) and **Compare Log to Commit…** (prompts for any commit SHA, tag, or ref), open the existing two-log comparison panel against the saved session captured at that commit. The commit is used as a *time label*: the baseline is resolved by matching the commit against the `git` / `buildCi` SHA recorded in each session's metadata (most recent capture wins on ties), not by `git show` — most logs live only under the log directory and are never tracked in Git. When no saved session matches the commit, the command says so and points at "Compare Logs" for a manual pick rather than silently diffing an arbitrary previous file. Short session SHAs (`rev-parse --short`) match the full resolved ref by prefix. ([commands-comparison-git.ts](src/commands-comparison-git.ts), [git-rev-resolve.ts](src/modules/git/git-rev-resolve.ts), [git-baseline.ts](src/modules/compare/git-baseline.ts), [baseline-match.ts](src/modules/compare/baseline-match.ts), [session-commit-from-meta.ts](src/modules/session/session-commit-from-meta.ts))
- **Localized the new compare-to-commit and external-reload strings** — the 8 new user-facing strings from the two features above (the command title, the Git-ref prompt, the bad-ref / no-baseline / no-current-log messages, and the disk-changed / disk-deleted reload prompts) are now hand-translated into all 10 non-English locales (de, es, fr, it, ja, ko, pt-br, ru, zh-cn, zh-tw), bringing every bundle back to 1566/1566 keys.

<details>
<summary>Maintenance</summary>

**Documentation**

- **`ROADMAP.md` is now a one-line redirect to the plans folder on GitHub** — the roadmap tables (core features, scored/unscored future work, known issues) each linked individual `plans/**.md` files that broke or went stale on every rename, archive, or ship. The whole file is replaced with a single pointer to [the plans folder on GitHub](https://github.com/saropa/saropa-log-capture/tree/main/plans), the live source of truth. ([ROADMAP.md](ROADMAP.md))
- **All non-user-facing changelog entries are now grouped under collapsible `<details>` Maintenance sections within their release** — build-tooling, test, file-modularization, internal-refactor, packaging, CI, and developer-docs entries that previously sat in `Added` / `Changed` / `Fixed` (or in ad-hoc `### Tests` / `### Administration` / `### Documentation` / `### Refactored` sections) are moved into a per-release `Maintenance` block, so each release's user-facing list reads clean while the engineering detail stays available on expand. Applied across the active changelog and the full archive. ([CHANGELOG.md](CHANGELOG.md), [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md))

**Build tooling**

- **`publish.py` Step 9 no longer runs any translation — it audits locale bundles and stops** — the l10n step used to fill missing strings by calling the machine-translation pipeline (offline NLLB, else Google) during every release. That is removed: `check_l10n_bundles` now only re-syncs the English source bundle (mechanical key alignment, not translation) and reports the missing / still-English strings plus a CSV worklist. When any locale is incomplete the step returns False and the orchestrator prompts **retry / ignore / abort** (default retry): retry re-audits after you fill gaps by hand with `python scripts/translate_l10n.py`, ignore continues the release with gaps, abort stops it. Non-interactive / piped runs ignore gaps (preserving the prior non-fatal behavior) instead of hanging on input or looping on retry. Reason: an unattended NLLB/GPU translation job must never start as a side effect of publishing. Build tooling only. ([checks_build.py](scripts/modules/publish/checks_build.py), [orchestrator.py](scripts/modules/publish/orchestrator.py), [publish.py](scripts/publish.py))

</details>

---

## [8.0.2]

The viewer interface now ships translations in 10 languages, plus internal translation-tooling polish and a security dependency bump. [log](https://github.com/saropa/saropa-log-capture/blob/v8.0.2/CHANGELOG.md)

### Changed

- **The viewer UI now ships translations in 10 languages** — German (de), Spanish (es), French (fr), Italian (it), Korean (ko), Portuguese – Brazil (pt-br), Chinese – Simplified (zh-cn), and Chinese – Traditional (zh-tw) are 100% complete across all 1,558 strings; Japanese (ja) and Russian (ru) each have a single string still untranslated (99.9%). The dozen-or-so strings that remain English in every locale are brand/product names and developer-facing text kept English by design (identity mappings). ([l10n/bundle.l10n.de.json](l10n/bundle.l10n.de.json), [l10n/bundle.l10n.es.json](l10n/bundle.l10n.es.json), [l10n/bundle.l10n.fr.json](l10n/bundle.l10n.fr.json), [l10n/bundle.l10n.it.json](l10n/bundle.l10n.it.json), [l10n/bundle.l10n.ja.json](l10n/bundle.l10n.ja.json), [l10n/bundle.l10n.ko.json](l10n/bundle.l10n.ko.json), [l10n/bundle.l10n.pt-br.json](l10n/bundle.l10n.pt-br.json), [l10n/bundle.l10n.ru.json](l10n/bundle.l10n.ru.json), [l10n/bundle.l10n.zh-cn.json](l10n/bundle.l10n.zh-cn.json), [l10n/bundle.l10n.zh-tw.json](l10n/bundle.l10n.zh-tw.json))

<details>
<summary>Maintenance</summary>

**Security**

- **Security: bumped transitive `shell-quote` to 1.8.4** — cleared the critical advisory GHSA-w7jw-789q-3m8p (`quote()` failed to escape newlines in object `.op` values). Dev/transitive dependency only; no shipped runtime change. ([package-lock.json](package-lock.json))

**Build tooling**

- **`publish.py` now gates the `@types/vscode` ↔ `engines.vscode` version contract in Step 1, before anything is mutated** — vsce refuses to package when the declared `@types/vscode` range is newer than `engines.vscode` (e.g. `^1.120.0` vs `^1.105.0`), but it only checks this at the Package step, which runs *after* Step 10 has already bumped `package.json` and stamped the CHANGELOG — so the reject abandoned a half-finished release (the 8.0.2 run, 2026-06-10). A new `Manifest compat` prerequisite runs the same static comparison up front and, on a mismatch, prints the problem plus suggested fixes with an **accept / retry / ignore / exit** prompt: accept lowers `@types/vscode` to the engine floor (the backward-compatible direction, since raising the engine would silently drop users on older VS Code), retry re-checks after a manual edit, ignore proceeds anyway, exit stops with nothing written. Non-interactive runs (CI / `--yes`) fail fast with the diagnosis instead of looping. Build tooling only. ([checks_prereqs.py](scripts/modules/publish/checks_prereqs.py), [display.py](scripts/modules/publish/display.py), [orchestrator.py](scripts/modules/publish/orchestrator.py), [publish.py](scripts/publish.py))
- **`publish.py` now verifies the CHANGELOG release intro and repoints its log link to the release tag** — Step 10 (Version & CHANGELOG) checks that the `## [version]` section opens with the plain-language intro line that ends with a `log](…/blob/vX.Y.Z/CHANGELOG.md)` link, and rewrites that link from `…/blob/main/…` (the in-progress placeholder) to the version being released. If the intro line is missing it prompts retry/ignore/abort (default retry) so the developer can add it and re-read without losing the run; non-interactive runs warn and continue instead of looping. Build tooling only. ([version.py](scripts/modules/publish/version.py), [publish.py](scripts/publish.py))

**Localization tooling**

- **More of the viewer UI is now translatable** — surfaces that were previously hardcoded English are routed through the localization pipeline so they translate with the rest of the UI: the About panel's marketing copy (tagline, blurb, project/connect link descriptions), the Export modal, the Integrations panel chrome, the Collections panel (empty state, relative-time and source-count labels, row actions), the Keyboard Shortcuts reference (every section, header, action, and description), and the entire right-click context menu (every label and tooltip). Brand/product names, version badges, URLs, keyboard hints, and developer/debug strings stay English by design. This adds ~337 strings to the translation pipeline (new registries `strings-viewer-e/f/g.ts` and `strings-webview.ts` additions); they render in English today and pick up translations on the next translate run. ([viewer-about-panel.ts](src/ui/viewer-panels/viewer-about-panel.ts), [viewer-export-html.ts](src/ui/viewer-panels/viewer-export-html.ts), [viewer-integrations-panel-html.ts](src/ui/viewer-panels/viewer-integrations-panel-html.ts), [viewer-collections-panel-script.ts](src/ui/viewer-panels/viewer-collections-panel-script.ts), [viewer-keyboard-shortcuts-html.ts](src/ui/viewer-panels/viewer-keyboard-shortcuts-html.ts), [viewer-context-menu-html.ts](src/ui/viewer-context-menu/viewer-context-menu-html.ts))
- **`translate_l10n.py` can now be cancelled gracefully — CTRL-C saves progress and resumes** — a multi-hour run no longer loses the locale in flight when interrupted. `translate_locale` moved its orphan-prune + bundle/provenance save into a `finally`, so `KeyboardInterrupt` (a `BaseException`, so the retry/apply `except Exception` blocks never swallow it) persists everything translated so far before propagating; `run_translate` catches it and prints `Cancelled — progress saved… Re-run to resume` instead of a traceback. Because a re-run keeps already-translated keys (`gaps` skips non-English values, `low_quality` skips `nllb`-provenance keys), cancellation is now a pause, not a loss. Build tooling only. ([l10n_translator.py](scripts/modules/verify/l10n_translator.py), [l10n_actions.py](scripts/modules/verify/l10n_actions.py))
- **`translate_l10n.py` NLLB now runs on the GPU, shows a live progress bar, and no longer looks hung or crashed** — the engine now registers the pip-installed CUDA runtime DLL directories (`site-packages/nvidia/*/bin`) with the Windows loader via `os.add_dll_directory` before the device cascade. `ctranslate2` lazy-loads `cublas64_12.dll` at first inference but ships only cuDNN, so without this the CUDA device loaded yet the first translation failed and the run silently dropped to (much slower) CPU; cuBLAS comes from the already-present `nvidia-cublas-cu12` wheel, which Python 3.8+ never finds on `PATH` alone. Each locale's pass now renders a `\r` progress bar (`[####....] 42.0%  654/1558`) matching the audit coverage table, the one-time ~7 GB model load prints `[nllb] Loading NLLB-200-3.3B model (one-time, may take a minute)…` before the blocking load, and the device-cascade fallback line was reworded from `load failed` to `unavailable, trying next fallback` so a CUDA→CPU step no longer reads as a crash. Build tooling only. ([l10n_actions.py](scripts/modules/verify/l10n_actions.py), [l10n_nllb_engine.py](scripts/modules/verify/l10n_nllb_engine.py))

</details>

---

## [8.0.1]

The Flow Map can now show where you left the app for an external app or API, right-click submenus also fit narrow panels, dragging a log file in is more dependable, plus an internal localization-tooling change. [log](https://github.com/saropa/saropa-log-capture/blob/main/CHANGELOG.md)

### Added

- **Flow Map shows off-app handoffs** — when the app emits the new `[flowmap] handoff <api|app> "Name"` tag, leaving for an external application (maps, dialer, browser, share sheet) or making an outbound API call now appears in the Flow Map as a distinct ↗️ leaf node off the screen it left from, with its source link, instead of vanishing from the journey. Handoffs are leaf side-exits: the screen they branch from keeps its own dwell, visit count, and the edge to wherever the user goes next, so a handoff never steals the screen's time. `api` handoffs read distinctly from `app` launches (the type rides in the label) and external nodes get their own dashed style across the diagram, SVG, and report tables. See [plans/guides/flowmap-tag-navigation.md](plans/guides/flowmap-tag-navigation.md). ([flow-map-model.ts](src/modules/flow-map/flow-map-model.ts), [flow-map-breadcrumbs.ts](src/modules/flow-map/flow-map-breadcrumbs.ts), [flow-map-builder.ts](src/modules/flow-map/flow-map-builder.ts), [flow-map-format.ts](src/modules/flow-map/flow-map-format.ts), [flow-map-mermaid.ts](src/modules/flow-map/flow-map-mermaid.ts), [flow-map-svg.ts](src/modules/flow-map/flow-map-svg.ts))

### Fixed

- **Dragging a log file onto the Logs panel is more reliable** — the drop hint overlay no longer flickers off the moment the cursor passes over a row (it now tracks drag enter/leave depth instead of hiding on the first boundary crossing), and a drag you cancel with Escape or release outside the window no longer strands the dashed overlay on screen (drag-end, window-blur, and any handler error all reset it). The drop still loads the file by its OS path, by `file://`/`http(s)://` URI in the drag payload, or by reading the dropped contents — whichever the host exposes. ([viewer-drop-to-open.ts](src/ui/viewer/viewer-drop-to-open.ts))
- **Right-click submenus no longer run off the right edge in a narrow panel** — the submenu placement maximized height to the whole window and scrolled when too tall, but did the same for width only on the side that fit; a flyout wider than a narrow terminal split (or with a scrollbar) still clipped off the right edge. Width is now treated the same as height: the flyout is capped to the window width and scrolls horizontally instead of clipping, and the height cap is applied before the width is measured so a scrolling flyout's scrollbar is counted in the flip/clamp. ([viewer-context-menu-position.ts](src/ui/viewer-context-menu/viewer-context-menu-position.ts))

<details>
<summary>Maintenance</summary>

**Localization tooling**

- **Translation pipeline can now use offline NLLB-200-3.3B for higher-quality locale strings** — `translate_l10n.py` automatically prefers Meta's offline NLLB-200-3.3B model (via CTranslate2) over Google Translate when the model is cached, with Google as an automatic fallback and the rest of the pipeline (brand shielding, validation, bundle merge, audit) unchanged. The model is never auto-downloaded; enable it with `pip install ctranslate2 sentencepiece huggingface_hub` + a one-time `huggingface-cli download JustFrederik/nllb-200-3.3B-ct2-float16`, or set `SAROPA_SKIP_NLLB=1` to force Google. Build tooling only; no regenerated translations ship in this change. ([l10n_nllb_engine.py](scripts/modules/verify/l10n_nllb_engine.py), [l10n_translator.py](scripts/modules/verify/l10n_translator.py), [translate_l10n.py](scripts/translate_l10n.py))
- **`translate_l10n.py` is colorized and split into focused modules** — the translation CLI now prints colored output (green = complete, yellow = gap, red = missing/error, cyan = headers) reusing the shared `modules.publish.constants` palette, and the ~300-line monolith was broken into `l10n_console.py` (color helpers), `l10n_audit_display.py` (status/coverage/gap rendering), `l10n_actions.py` (sync/translate/report), and `l10n_cli.py` (menu + `--run-mode`), leaving `translate_l10n.py` a thin launcher. Behavior is unchanged; build tooling only. ([l10n_console.py](scripts/modules/verify/l10n_console.py), [l10n_audit_display.py](scripts/modules/verify/l10n_audit_display.py), [l10n_actions.py](scripts/modules/verify/l10n_actions.py), [l10n_cli.py](scripts/modules/verify/l10n_cli.py), [translate_l10n.py](scripts/translate_l10n.py))
- **Translation provenance + a low-quality upgrade pass** — the pipeline now records which engine produced each locale string in a per-locale sidecar (`l10n/provenance/<locale>.json`) and the audit shows a per-locale engine breakdown with a high/low quality split. Following the Saropa Contacts quality model, NLLB / manual / verified-identity count as high quality and Google / other free MT / **untracked** (no record) count as low. The translation menu now offers two distinct modes: **translate gaps** (fill untranslated strings) and **upgrade low-quality → NLLB** (re-translate only the weak / untracked strings, e.g. all the existing Google output, leaving high-quality ones untouched); `--scope gaps|low_quality` exposes the same to CI. A failed NLLB upgrade keeps the prior translation rather than overwriting it with English. Build tooling only. ([l10n_provenance.py](scripts/modules/verify/l10n_provenance.py), [l10n_translator.py](scripts/modules/verify/l10n_translator.py), [l10n_bundle_audit.py](scripts/modules/verify/l10n_bundle_audit.py), [l10n_audit_display.py](scripts/modules/verify/l10n_audit_display.py), [l10n_actions.py](scripts/modules/verify/l10n_actions.py), [l10n_cli.py](scripts/modules/verify/l10n_cli.py))

**Test tooling**

- **Quieter test runs — `min` reporter instead of the full per-test list** — `npm run test` no longer floods the terminal with hundreds of passing `✔` lines; the Mocha reporter is set to `min`, which prints only the run summary and any failure details (with full stack traces). Test tooling only; no shipped behavior change. ([.vscode-test.mjs](.vscode-test.mjs))
- **Test fix: session-manager mock gained the `fileUri` it now requires** — the `SessionManagerImpl` "capture all output" test mocked a session without a `fileUri`, but `broadcastLine` started reading `session.fileUri.fsPath` in b916c032 (per-file letter codes), so the test threw `Cannot read properties of undefined (reading 'fsPath')`. The mock now supplies a `fileUri`. Test only; no shipped behavior change. ([session-manager.test.ts](src/test/modules/session/session-manager.test.ts))

</details>

---

## [8.0.0]

The Logs panel got a big overhaul — pin logs to the top, see active filters as chips, find big logs with a size filter, and runs now nest under their project as a tree; you can open any log by path or URL (or just drag one in), every run shows by default with "Latest only" as an opt-in, the new Export Session Flow Map command turns a log into a screen-journey report with an activity chart, markdown reports render properly on open, and deep reports folders load much faster. [log](https://github.com/saropa/saropa-log-capture/blob/v8.0.0/CHANGELOG.md)

### Changed

- **The Flow Map report is easier to read and rearrange** — drag the divider between the diagram and the detail column to trade width between them, and collapsing every section in a column now shrinks that whole column aside so the open side gets the room (the same way the single Flow section already behaved). Sections were renamed for clarity: **Narrative → Executive Summary** (now two sentences longer, with a copy icon that appears on hover to grab the whole summary), **Activity → Activity Timeline**, **Screen dwell → Screen Visit Log**, and **Performance · warnings · errors → Issue Report**. The Issue Report table is now sortable — click any column header (a chevron hints on hover) to sort by time, severity, source, and so on. ([flow-map-html.ts](src/modules/flow-map/flow-map-html.ts), [flow-map-report.ts](src/modules/flow-map/flow-map-report.ts), [flow-map-panel.ts](src/ui/panels/flow-map-panel.ts), [flow-map-panel-styles.ts](src/ui/panels/flow-map-panel-styles.ts), [flow-map-panel-script.ts](src/ui/panels/flow-map-panel-script.ts))
- **Logs list shows every run by default again; "Latest only" is now an opt-in toggle** — auto-folding older same-name runs behind a "+N older" badge read as missing logs and confused people, so the default list now shows every run. Turn on **"Latest only"** in the options (⋮) menu if you want the folded view. ([session-display.ts](src/ui/session/session-display.ts), [viewer-session-panel.ts](src/ui/viewer-panels/viewer-session-panel.ts))
- **Tidier Logs options menu that fits short panels** — the options (⋮) menu is now grouped into **Filter**, **Display**, and **Actions** submenus, so the top-level menu stays short instead of running off the bottom of a short panel. The **Tags** filter now lives inside **Filter** alongside date and size (it is a filter), so the top level is a clean three groups with no stray toggle. Each submenu opens as a flyout placed against the window edges (it flips and scrolls as needed) so it is never cut off. The date and size filters also read **"Filter by date"** and **"Filter by size"** now. ([viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts), [viewer-session-options-menu.ts](src/ui/viewer-panels/viewer-session-options-menu.ts), [viewer-styles-session-options.ts](src/ui/viewer-styles/viewer-styles-session-options.ts), [strings-viewer-b.ts](src/l10n/strings-viewer-b.ts))
- **The Logs panel no longer hides itself** — the session-history list used to disappear two ways: a 5-second countdown after you opened a file, and an outside-click rule that closed it the moment you clicked anywhere in the log viewer. Both made browsing several files in a row annoying. Both are gone — the panel now stays open until you explicitly close it (its close button, the Logs icon, Escape, or opening another panel). ([viewer-session-panel-events.ts](src/ui/viewer-panels/viewer-session-panel-events.ts), [viewer-session-panel.ts](src/ui/viewer-panels/viewer-session-panel.ts))
- **The Logs name filter is now cumulative — "Hide This Name" stacks, instead of replacing the last one** — right-clicking **"Hide This Name"** on several sessions now hides all of them at once rather than swapping to whichever you picked last. The filter bar shows one removable pill per name with an **[x]** to drop just that name from the filter; **"Show All"** still clears everything. "Show Only This Name" works the same way (show sessions matching any of the chosen names), and switching between hide and show-only starts a fresh list. ([viewer-session-panel.ts](src/ui/viewer-panels/viewer-session-panel.ts), [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts), [viewer-session-panel-events.ts](src/ui/viewer-panels/viewer-session-panel-events.ts), [viewer-styles-session-list.ts](src/ui/viewer-styles/viewer-styles-session-list.ts))
- **ASCII art blocks (e.g. the Drift debug-server banner) no longer draw a left gutter rail** — the continuous severity border down the left of a box-drawing block was shifting the box and reading as a stray vertical line that broke the layout. The block's severity is conveyed by its yellow tint and rounded border; the rail is gone. ([viewer-styles-ascii-art.ts](src/ui/viewer-styles/viewer-styles-ascii-art.ts))
- **Log rows now lay their gutter columns out as a true grid, so decorations can't overlap the message** — the line number, timestamp, session-elapsed, PID/TID, level, and tag are each their own clipping column with the message in its own track, instead of the previous estimated-width prefix that could let a long timestamp or tag paint over the start of the message text. Fixed-width columns are sized to their exact character count; a long tag clips with an ellipsis (full value on hover) rather than spilling. This is the first step of a viewer-wide column rewrite (plan 055): regular log lines move to the grid now; AI rows, multi-frame stack headers, and chips still use the prior layout and follow in later steps. Reusable so CSV and markdown tables can adopt the same overlap-proof columns later. ([viewer-styles-columns.ts](src/ui/viewer-styles/viewer-styles-columns.ts), [viewer-deco-content.ts](src/ui/viewer-decorations/viewer-deco-content.ts), [viewer-decorations.ts](src/ui/viewer-decorations/viewer-decorations.ts), [viewer-data-helpers-render.ts](src/ui/viewer/viewer-data-helpers-render.ts))
- **The Logs list is now a Controller-rooted tree, with an opt-in "Latest only" filter** — when a project runs it emits its own app log plus a cluster of peripheral tool logs (Saropa Lint Report, Arb Translate, L10n Failures, Drift Advisor). Those used to scatter as sibling rows. Each day now groups under the project's own session — the **Controller** — with the peripheral logs nested beneath it; a peripheral attaches to the nearest earlier Controller of that day, and any peripheral with no preceding run lists flat at the bottom. The Controller is detected automatically by matching the workspace folder name, and you can pin or demote any log via the new right-click **"Set as Controller" / "Mark as Peripheral"** actions (or the `saropaLogCapture.controllerNames` setting). "Latest only" is an opt-in toggle (off by default, since auto-folding older runs read as missing logs) that no longer hides older logs without a trace when enabled: the newest log of each name shows a clickable **"+N older"** badge that expands the rest in place. "Latest only" folds **peripheral** logs only — a Controller (the project's own session, e.g. "contacts") is never collapsed, so every run of it stays visible whether grouped or not. This replaces the per-day Reports bucket. ([viewer-session-panel-controllers.ts](src/ui/viewer-panels/viewer-session-panel-controllers.ts), [session-kind-classifier.ts](src/modules/session/session-kind-classifier.ts), [viewer-session-context-menu.ts](src/ui/viewer-context-menu/viewer-session-context-menu.ts), [viewer-handler-sessions.ts](src/ui/provider/viewer-handler-sessions.ts), [viewer-session-transforms.ts](src/ui/viewer/viewer-session-transforms.ts))

### Added

- **README refreshed for v8** — adds a dedicated **Flow Map** section (Executive Summary, Activity Timeline, Screen Visit Log, sortable Issue Report) and documents the new Logs panel features (Controller-rooted tree, "Latest only" opt-in, active-filter chips, size filter, grouped options menu, recently-opened files, open-by-path/URL/drag) in Why Use This, the Features list, and Key Commands; adds two explanatory ASCII diagrams (Flow Map report layout, Logs panel Controller tree), a footer showing the release version and last-updated date; tightens surrounding prose with no loss of content. ([README.md](README.md))
- **The Logs options (⋮) menu now lists your 10 most recently opened files for one-click re-opening** — under a separator at the bottom of the menu, the files you've loaded via **Open log file…** / **Open log from URL…** appear newest-first; click one to re-open it in the viewer without re-browsing for it (handy for logs that live outside your reports folder). Until you've opened one, a dim **"Opened files will appear here"** notice sits in its place. ([viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts), [viewer-session-options-menu.ts](src/ui/viewer-panels/viewer-session-options-menu.ts), [viewer-session-panel-events-messages.ts](src/ui/viewer-panels/viewer-session-panel-events-messages.ts), [viewer-styles-session-options.ts](src/ui/viewer-styles/viewer-styles-session-options.ts), [strings-viewer-b.ts](src/l10n/strings-viewer-b.ts))
- **The Logs panel now shows which filters are active** — when a date, size, or name filter is on, a bar under the header lists each as a removable chip (e.g. **"Last 7 days ✕"**, **"Larger than 1 MB ✕"**) — click the ✕ to clear just that one. A small dot also appears on the options (⋮) button — and on the **Filter** group inside the menu — so you can tell filters are applied even after the bar scrolls out of view, and see which group to open to change them. Previously the date and size filters were silent: the list just looked short with no explanation. ([viewer-session-panel-rendering-name-filter.ts](src/ui/viewer-panels/viewer-session-panel-rendering-name-filter.ts), [viewer-session-panel-events.ts](src/ui/viewer-panels/viewer-session-panel-events.ts), [viewer-styles-session-name-filter.ts](src/ui/viewer-styles/viewer-styles-session-name-filter.ts), [viewer-styles-session-options.ts](src/ui/viewer-styles/viewer-styles-session-options.ts))
- **Pin a log to the top of the Logs panel for instant access** — right-click any log and choose **"Pin to Top"** (and **"Unpin from Top"** to remove it). Pinned logs gather in a **Pinned** section at the very top of the list, newest pin first, and stay there regardless of the date, size, name, or tag filters — a pin always wins. Pinning captures the log's full metadata (line count, error/warning counts, duration, timestamps) into the central store at pin time, so a pinned row always lists and opens from cache with no file re-read, even across a months-deep archive. ([session-pin.ts](src/ui/session/session-pin.ts), [session-metadata.ts](src/modules/session/session-metadata.ts), [viewer-handler-sessions.ts](src/ui/provider/viewer-handler-sessions.ts), [viewer-session-context-menu.ts](src/ui/viewer-context-menu/viewer-session-context-menu.ts), [viewer-session-panel-rendering-pinned.ts](src/ui/viewer-panels/viewer-session-panel-rendering-pinned.ts), [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts))
- **New "minimum size" filter in the Logs panel for finding large logs** — the kebab options menu now has a size dropdown alongside the date-range one. Pick a lower bound (Larger than 25 KB / 50 KB / 100 KB / 500 KB / 1 MB / 5 MB / 10 MB / 50 MB, or "Any size") and the list hides everything smaller, so a heavy log is easy to spot in a busy folder. The choice persists with your other Logs display options and combines with the date, name, and tag filters. ([viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts), [viewer-session-panel-events.ts](src/ui/viewer-panels/viewer-session-panel-events.ts), [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts), [session-display.ts](src/ui/session/session-display.ts))
- **The live Logs view labels each accumulated file with a letter code so copies stay traceable** — the live viewer keeps streaming across every debug run in the window, so it can show thousands of lines while the file named in the footer holds only the latest run. Now, when the view spans two or more origin files, the gutter number is prefixed with a per-file letter (`A1139`, `B12`) that resets per file, and the footer shows a clickable **(n)** count of files in the view. Clicking it opens a **Files in this view** dialog listing each file by letter, name, line count, and time range; picking one opens that file's copy/open actions. Copying lines now carries provenance: each line gets its `A1139` code and the block is topped with a legend mapping every code to its absolute path — so a pasted excerpt always says which log it came from. Single-file views are unchanged (bare numbers, no legend). ([viewer-file-code-stamp.ts](src/ui/viewer/viewer-file-code-stamp.ts), [viewer-files-list-modal.ts](src/ui/viewer/viewer-files-list-modal.ts), [viewer-deco-content.ts](src/ui/viewer-decorations/viewer-deco-content.ts), [viewer-copy.ts](src/ui/viewer/viewer-copy.ts), [viewer-log-file-modal.ts](src/ui/viewer/viewer-log-file-modal.ts), [session-manager-events.ts](src/modules/session/session-manager-events.ts))
- **New "Export Session Flow Map" command turns a log into a screen-journey report** — a toolbar button (left of the filter and signals icons, replacing the redundant in-log search icon there — the editor title-bar `$(search)` still opens that search) opens a native dashboard-style report panel for the current session: colored stat pills (screens, duration, slow queries, repeat batches, crashes) in a sticky top bar beside the save icon, a section table-of-contents, and collapsible sections. The flow diagram (boxes for each screen/tab/dialog, one-way arrows with visit and transition counts, the crash node pulsing red) sits beside the narrative on wide panels and stacks when narrow; the screen-dwell table has proportional duration bars and the performance/warnings/errors table is severity-accented, both with clean row-separator styling. Every row links back to **both** its source `file:line` (opens in the editor) **and** its originating **log line** — click to reveal it in the log viewer or copy the raw line; clicking a diagram node highlights its table row and jumps the log to where that screen was entered. A top-right **Save as Markdown** icon writes the portable file. The diagram is drawn as a self-contained SVG (no extra dependency, works offline). Where the app prints navigation breadcrumbs it reconstructs the walked path; it recovers the crashing widget's `file:line` straight from Flutter's error report even when no breadcrumb was logged, and (for projects whose screens follow a discoverable convention) joins each screen to its source file. ([commands-flow-map.ts](src/commands-flow-map.ts), [flow-map-panel.ts](src/ui/panels/flow-map-panel.ts), [flow-map-svg.ts](src/modules/flow-map/flow-map-svg.ts), [flow-map-html.ts](src/modules/flow-map/flow-map-html.ts), [flow-map-builder.ts](src/modules/flow-map/flow-map-builder.ts), [flow-map-source-scan.ts](src/modules/flow-map/flow-map-source-scan.ts), [viewer-toolbar-html.ts](src/ui/viewer-toolbar/viewer-toolbar-html.ts))
- **The Flow Map gains an Activity chart that jumps to busy moments in the log** — a new **📈 Activity** section (above **Screen dwell**) plots how many session events landed over time as a single line: the vertical axis counts events, the horizontal axis is wall-clock time, with basic axis numbering on both. Each plotted point is clickable — clicking it reveals the first log line from that moment in the log viewer, so a spike in the chart takes you straight to the noisy stretch. Untimed entries are excluded so the timeline isn't skewed to the origin; sessions with fewer than two timed events show a short note instead of an empty chart. ([flow-map-activity-chart.ts](src/modules/flow-map/flow-map-activity-chart.ts), [flow-map-html.ts](src/modules/flow-map/flow-map-html.ts), [flow-map-panel-script.ts](src/ui/panels/flow-map-panel-script.ts), [flow-map-panel-styles.ts](src/ui/panels/flow-map-panel-styles.ts))
- **New "Copy Error JSON" right-click action for analysts** — alongside the existing "Copy Error" / "Copy Warning" item, right-clicking an error or warning block now also offers "Copy Error JSON" (labelled "Copy Warning JSON" on warnings). It copies a structured JSON object with the full error block in an `error` field plus the absolute `logPath`, `logFile`, severity `level`, 1-based `lineStart`/`lineEnd`, the block's first `timestamp`, and the captured `session` details — so the full context travels in a single paste. ([viewer-context-menu-html.ts](src/ui/viewer-context-menu/viewer-context-menu-html.ts), [viewer-context-menu.ts](src/ui/viewer-context-menu/viewer-context-menu.ts), [viewer-context-menu-line-actions.ts](src/ui/viewer-context-menu/viewer-context-menu-line-actions.ts), [viewer-message-handler-actions.ts](src/ui/provider/viewer-message-handler-actions.ts))
- **Open any log file by path, and drag-and-drop logs from the OS** — two new ways to load a file that bypass the folder-scan session list. A new **"Open log file…"** entry in the Logs panel kebab menu (under "Export session list to JSON…") and a matching **"Saropa Log Capture: Open Log File…"** command palette entry open a native file picker and load the chosen file directly — including logs outside the configured reports folder, which the "browse session root" picker could never open by path. You can also **drag a log file from your file manager straight onto the viewer**: a drop hint appears, the file loads on release, and a toast confirms which file opened. The drop is captured at the window level (so the editor-drop overlay can't swallow it) and listens for `dragenter`+`dragover` so the drop actually fires; if the OS sandbox exposes no file, a message points you to the kebab → Open log file… instead of failing silently. ([commands-session.ts](src/commands-session.ts), [viewer-drop-to-open.ts](src/ui/viewer/viewer-drop-to-open.ts), [viewer-dropped-log.ts](src/ui/provider/viewer-dropped-log.ts), [viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts), [viewer-session-options-menu.ts](src/ui/viewer-panels/viewer-session-options-menu.ts))
- **Open a log straight from a URL** — a new **"Open log from URL…"** kebab entry and **"Saropa Log Capture: Open Log from URL…"** command prompt for an http(s) link, download it to the extension's temp storage, and render it in the viewer — handy for a CI artifact, a raw gist, or an internal dashboard link you'd otherwise have to save by hand first. Dragging a web link straight onto the viewer does the same. The download is bounded (30s timeout, 50 MB cap, http/https only) and reports a toast on success or a clear reason on failure. ([viewer-url-log.ts](src/ui/provider/viewer-url-log.ts), [commands-session.ts](src/commands-session.ts), [viewer-dropped-log.ts](src/ui/provider/viewer-dropped-log.ts), [viewer-drop-to-open.ts](src/ui/viewer/viewer-drop-to-open.ts), [viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts))
- **Files you open with "Open Log File" now stay in the Logs list, grouped by the day you loaded them** — opening a file by path used to be transient: the file showed in the viewer but never appeared in the Logs history, so a log from another project or an archived file off in a folder somewhere left no trace once you moved on. Each such file now becomes a row in the Logs list under the day you opened it, marked with a small folder icon (hover: "Opened via Open Log File"), carrying the same line-count and error/warning badges as captured sessions. Re-opening the same file moves its one row to the new day. The record (and its cached counts, so the list never re-reads the file) lives in `.loaded-files-history.json` in your reports folder; a file that already lives in the reports folder is shown once, in its normal place, never duplicated. ([loaded-files-history.ts](src/modules/session/loaded-files-history.ts), [loaded-file-metadata.ts](src/modules/session/loaded-file-metadata.ts), [commands-session.ts](src/commands-session.ts), [session-history-fetching.ts](src/ui/session/session-history-fetching.ts), [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts))

### Fixed

- **A log line and its stack trace are now ONE collapsible row, not two** — every `[log]` line that carried a stack used to render the message AND a separate collapsed `▶` row for the trace's first frame, so a startup sequence of ten such entries showed ten extra sub-rows that read as nested "subgrouping". The message line itself is now the toggle: it carries the `▶`/`▼` chevron and the whole trace folds under it as a single level (click the chevron or the message row to expand; frames are children, no separate stack-header row). Standalone traces with no owning log line still group as their own unit, and Drift `Drift: Sent …` SQL traces keep their existing repeat-collapse. Stack frames are also clickable across the whole row to open their source, and a frame sitting just before filter-hidden lines (e.g. device "warnings & errors only" rows) now shows a reveal chevron instead of swallowing them silently. ([viewer-data-add-stack-ingest.ts](src/ui/viewer/viewer-data-add-stack-ingest.ts), [viewer-data-divider.ts](src/ui/viewer/viewer-data-divider.ts), [viewer-script-click-handlers.ts](src/ui/viewer/viewer-script-click-handlers.ts), [viewer-data-helpers-render-stack.ts](src/ui/viewer/viewer-data-helpers-render-stack.ts))
- **Markdown reports now render on open instead of showing raw text** — opening a generated session report (or any `.md`/`.json`/`.csv` file) in the viewer left it as raw source until you found and clicked the format-toggle button, so a flow-map report looked like an unformatted log dump. Structured documents now auto-enable formatting on open (the toggle still turns it back off). Fenced code blocks (```` ```mermaid ````, ```` ```sql ````, etc.) also render as a clean monospace code block with a language label instead of being mangled by the bold/italic/link rules — the diagram and SQL source inside a report stay readable. ([viewer-script-messages.ts](src/ui/viewer/viewer-script-messages.ts), [viewer-toolbar-script.ts](src/ui/viewer-toolbar/viewer-toolbar-script.ts), [viewer-format-markdown.ts](src/ui/viewer/viewer-format-markdown.ts), [viewer-styles-format.ts](src/ui/viewer-styles/viewer-styles-format.ts))
- **Rendered markdown looks like a document, not indented log lines** — several layout fixes to the formatted markdown view: opening a markdown document switches the viewer to a comfortable line height (restoring your configured default on exit), so reports read as prose and the spacing still follows the font-size and line-height controls instead of a separate hidden setting; all lines share one left edge (the per-heading left borders and extra indents that made the text ragged are gone, and the unused log gutter is trimmed); headings get a taller, vertically-centered row with breathing room above and below; the section collapse arrow is now a subtle, right-aligned chevron instead of a bold left prefix; and pipe tables render as **aligned columns** (fixed monospace column widths, a bold header row with an underline, and the `|---|` separator row collapsed away) instead of raw `| … |` text. Heading rows are sized from the heading's own font so they never overlap the next line, and when line-number decorations are on, markdown lines show a left gutter with the file line number plus a compact structure tag (heading level, code block, table, quote, list). ([viewer-format-markdown.ts](src/ui/viewer/viewer-format-markdown.ts), [viewer-format-markdown-layout.ts](src/ui/viewer/viewer-format-markdown-layout.ts), [viewer-data-helpers-core.ts](src/ui/viewer/viewer-data-helpers-core.ts), [viewer-data-helpers-render.ts](src/ui/viewer/viewer-data-helpers-render.ts), [viewer-styles-format.ts](src/ui/viewer-styles/viewer-styles-format.ts))
- **Markdown readability pass — colored headings, comment folding, focused options** — building on the markdown document view: headings are now color-coded by level (and the same colors mark each heading on the scroll-map minimap so you can navigate the outline at a glance), with generous top spacing so a heading clearly starts a new section. HTML comments (`<!-- … -->`) are detected and shown in comment green with a `//` tag; a multi-line comment collapses to a single line via a leading chevron, and the `<!--`/`-->` delimiters are dropped from the display (the green color and `//` tag already signal a comment). Long lines now wrap in markdown even when the log view is in no-wrap mode, so prose is no longer clipped off the right edge. The gutter's structure-tag column is wider and left-aligned for legibility, and the decoration options menu now hides the toggles that don't apply to a document (severity dot/bar, timestamp, session-elapsed, PID/TID, level prefix), leaving just line numbers. The document line height is more generous and now survives the workspace `logLineHeight` setting (which previously could reset markdown back to dense spacing), and each top-level list item gets extra space above it so multi-line bullets read as separate items instead of one block. ([viewer-format-markdown.ts](src/ui/viewer/viewer-format-markdown.ts), [viewer-format-markdown-layout.ts](src/ui/viewer/viewer-format-markdown-layout.ts), [viewer-scrollbar-minimap-paint.ts](src/ui/viewer/viewer-scrollbar-minimap-paint.ts), [viewer-deco-settings-sync.ts](src/ui/viewer-decorations/viewer-deco-settings-sync.ts), [viewer-script-messages-typography.ts](src/ui/viewer/viewer-script-messages-typography.ts), [viewer-data-helpers-core.ts](src/ui/viewer/viewer-data-helpers-core.ts), [viewer-script-click-handlers.ts](src/ui/viewer/viewer-script-click-handlers.ts), [viewer-styles-format.ts](src/ui/viewer-styles/viewer-styles-format.ts))
- **Pointing the Logs list at a folder of large reports no longer blanks the panel** — browsing the session root to a folder that held multi-hundred-MB report sidecars (e.g. a 117 MB `l10n_failures.json`) showed "No sessions found". The list refresh read **every** tracked file in full just to parse a header line, so the giant files exhausted memory and the whole fetch silently fell back to an empty result. Files above 2 MB now get a quick head+tail read (header + SESSION END footer only — never the body), and the deferred severity scan skips bodies above 25 MB, so a folder of huge reports lists instantly instead of going blank. ([session-history-helpers.ts](src/ui/session/session-history-helpers.ts), [session-severity-scan.ts](src/ui/session/session-severity-scan.ts))
- **Large report archives load far faster** — pointing the Logs list at a months-deep `reports/` tree (observed: 3,955 files, 1,244 of them `.json`/`.csv`/`.html` reports incl. 50 over 2 MB) was very slow because the deferred severity scan read every file up to 25 MB and ran the log-level classifier over it — hundreds of MB of JSON/CSV that carry no log severities. The severity scan now runs on line-oriented log content only (`.log`/`.txt`); report sidecars still list, just without severity badges. ([config-file-utils.ts](src/modules/config/config-file-utils.ts), [session-severity-scan.ts](src/ui/session/session-severity-scan.ts))
- **Re-opening the Logs list is now near-instant** — the header pass (date / project / line-count / duration) is cached to the central metadata store keyed by each file's mtime+size, so a re-load reads that cache once and skips re-opening every file on disk. Only new or changed files are re-parsed; the cache write-back is a single batched write per refresh, not one per file. A changed file (different mtime/size) invalidates its entry and re-parses automatically. ([session-metadata.ts](src/modules/session/session-metadata.ts), [session-history-metadata.ts](src/ui/session/session-history-metadata.ts))
- **Opening and closing the Logs panel no longer re-scans everything each time** — the panel lives in a retained webview, but every open cleared the list and re-scanned the whole reports tree from scratch, so the delay returned on each open. It now re-renders the already-loaded list instantly from the retained state; the host is only hit on the first open, the Refresh button, or the active-session poll. The remaining first-load/refresh work is also faster: row hydration and the deferred severity scan now post to the webview in chunks (~100 rows per message) instead of one row at a time with a macrotask delay between each (thousands of round-trips for a deep archive), and the skeleton shows each file's size immediately (free from the stat pass) instead of an empty shimmer. ([viewer-session-panel.ts](src/ui/viewer-panels/viewer-session-panel.ts), [viewer-handler-wiring.ts](src/ui/provider/viewer-handler-wiring.ts), [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts))
- **Right-click submenus now use the whole screen and follow panel resizes** — expanded submenus (Copy & Export, Columns, Layout, …) were anchored to their menu row and capped to the room on one side of it, so a tall submenu or one opened near the bottom shrank to a tiny scrollable strip with empty screen beside it, and resizing the panel while the menu was open left it stranded. Each submenu is now placed in viewport coordinates: it spans the full available height, slides fully on-screen next to its trigger, and only scrolls when it genuinely cannot fit. The root menu gained the same viewport height cap, and both the menu and its open submenu reposition live when the panel is resized. ([viewer-context-menu-position.ts](src/ui/viewer-context-menu/viewer-context-menu-position.ts), [viewer-context-menu.ts](src/ui/viewer-context-menu/viewer-context-menu.ts), [viewer-styles-context-menu.ts](src/ui/viewer-styles/viewer-styles-context-menu.ts))

<details>
<summary>Maintenance</summary>

**Test tooling**

- **Test coverage for the Logs options menu, loaded-files menu, and session filters** — added unit tests for the grouped options (⋮) submenus and the recently-opened files list, refreshed the session name-filter, about-panel, and record-field tests, and corrected a stale toolbar-tooltip assertion (the search button moved out of the toolbar in the column-visibility work). Tests only; no user-facing behavior change. ([viewer-session-options-menu.test.ts](src/test/ui/viewer-session-options-menu.test.ts), [viewer-session-loaded-files-menu.test.ts](src/test/ui/viewer-session-loaded-files-menu.test.ts), [viewer-toolbar-tooltips.test.ts](src/test/ui/viewer-toolbar-tooltips.test.ts))

</details>

---

## [7.18.0]

SQL Query History now spans every log you've captured (not just the open one) and opens with a stats dashboard, live Drift Advisor database issues show up alongside it, every sidebar slide-out is resizable, the session list stops flashing two layouts while it loads, and the "Open Signals" notification button actually opens the panel. [log](https://github.com/saropa/saropa-log-capture/blob/v7.18.0/CHANGELOG.md)

### Changed

- **SQL Query History is now useful across every log, not just the open one** — the panel used to reset to "No parsed SQL fingerprints in this session yet" whenever the active log had no Drift output, even when your other logs had captured plenty. It now merges fingerprints aggregated across **all** your captured logs by default. A new **"Current session only"** filter (in the panel toolbar) scopes the view back to just the active log when you want that. ([viewer-sql-query-history-core.ts](src/ui/viewer-stack-tags/viewer-sql-query-history-core.ts), [viewer-sql-query-history-panel-html.ts](src/ui/viewer-panels/viewer-sql-query-history-panel-html.ts), [viewer-sql-query-history-panel-render.ts](src/ui/viewer-panels/viewer-sql-query-history-panel-render.ts), [viewer-sql-query-history-panel-helpers.ts](src/ui/viewer-panels/viewer-sql-query-history-panel-helpers.ts))

### Added

- **Cross-log SQL history stays fast as your log count grows** — the cumulative view is backed by a persisted index (`.saropa/cumulative-sql-index.json`) that updates incrementally as each session finalizes, so opening the panel reads one small file instead of re-scanning every session's metadata on every log switch. The index rebuilds itself automatically if it is missing, corrupt, or a log it tracked was deleted. ([cumulative-sql-fingerprint-index.ts](src/modules/db/cumulative-sql-fingerprint-index.ts), [cumulative-sql-fingerprint-index-store.ts](src/modules/db/cumulative-sql-fingerprint-index-store.ts), [cumulative-sql-fingerprint-refresh.ts](src/modules/db/cumulative-sql-fingerprint-refresh.ts), [session-drift-sql-fingerprint-persist.ts](src/modules/session/session-drift-sql-fingerprint-persist.ts))
- **SQL Query History now opens with a dashboard** — a compact strip at the top of the panel shows headline stats (distinct queries, total executions, slowest duration, logs covered) and a "Top queries by count" bar chart, all computed from the queries already in view so it adds no extra work. ([viewer-sql-query-history-dashboard.ts](src/ui/viewer-panels/viewer-sql-query-history-dashboard.ts), [viewer-styles-sql-query-history-dashboard.ts](src/ui/viewer-styles/viewer-styles-sql-query-history-dashboard.ts))
- **Live database issues from Drift Advisor surface in the SQL dashboard** — when a Saropa Drift Advisor debug server is reachable, the panel pulls its merged `/api/issues` list (missing-index suggestions and data anomalies) and lists them under the stats, each with a one-click "open the suggested SQL in the Drift viewer" action. The section stays hidden when no server is running. ([drift-advisor-issues-fetch.ts](src/modules/integrations/drift-advisor-issues-fetch.ts), [viewer-message-handler-panels.ts](src/ui/provider/viewer-message-handler-panels.ts))
- **Static Drift code issues from Saropa Lints, with a "turn the linters on" nudge** — the dashboard also reads Saropa Lints findings (extension API, or `reports/.saropa_lints/violations.json`) and lists Drift source-code rule violations (e.g. `WHERE`-less updates, unclosed databases). When the project clearly uses Drift but no Drift-rule findings are present — the Drift rule pack is off by default — it shows an "Enable Drift linters" action that opens a terminal pre-filled with the `saropa_lints` command (you press Enter; nothing runs automatically). ([drift-lint-violations.ts](src/modules/misc/drift-lint-violations.ts), [viewer-sql-query-history-dashboard.ts](src/ui/viewer-panels/viewer-sql-query-history-dashboard.ts))

### Fixed

- **Session list no longer flashes two different layouts while loading** — the panel painted a flat, ungrouped, filename-only shimmer list first, then replaced it with the day-grouped final list, so opening it on a cold cache reflowed jarringly and the shimmer rows could linger for a long time (every file was fully read before the grouped list appeared). The loading skeleton is now built from a cheap stat pass that carries each file's modified-time, so it paints in the **same day-grouped structure** as the final list and each row hydrates in place as its details load — one structure, no reflow. ([session-history-metadata.ts](src/ui/session/session-history-metadata.ts), [session-history-fetching.ts](src/ui/session/session-history-fetching.ts), [viewer-handler-wiring.ts](src/ui/provider/viewer-handler-wiring.ts), [viewer-session-panel-rendering-stream.ts](src/ui/viewer-panels/viewer-session-panel-rendering-stream.ts))
- **"Dismiss" on the new-logs banner now responds instantly** — the button waited for the host to re-scan the whole log folder before the banner cleared, so on a busy folder it looked dead. It now hides the banner immediately and reconciles in the background. ([viewer-session-panel-events-newer.ts](src/ui/viewer-panels/viewer-session-panel-events-newer.ts))
- **Every sidebar slide-out is now resizable, not just Sessions** — the drag handle lived inside the Session History panel, which is hidden whenever another panel (SQL Query History, Bookmarks, Trash, Options, Filters, Crashlytics, Collections, Project State, Signals, About, Find) is the active one, so only Sessions could be widened. The single handle now sits on the shared `#panel-slot` container and resizes whichever panel is open. ([viewer-content-body.ts](src/ui/provider/viewer-content-body.ts), [viewer-session-transforms.ts](src/ui/viewer/viewer-session-transforms.ts), [viewer-styles.ts](src/ui/viewer-styles/viewer-styles.ts))
- **"Open Signals" on the recurring-signal toast now works** — the notification fires right after a capture finishes, when the Log Viewer view is closed, and the button only posted a message to an already-open view, so it landed nowhere. The command now focuses the view and waits (≤1s) for it to resolve before posting. ([commands-signals.ts](src/commands-signals.ts))
- **Scroll-map minimap no longer washed in light grey** — the minimap painted its full-canvas base with the translucent scrollbar-slider color (`rgba(100,100,100,0.26)`), a grey meant to sit on top of content, so the whole minimap read as a light-grey overlay over the severity ticks and SQL bands. The base now uses the editor background like VS Code's own minimap. ([viewer-scrollbar-minimap-paint.ts](src/ui/viewer/viewer-scrollbar-minimap-paint.ts))

---

## [7.17.5]

The highlight sweep across grouped ASCII-art log blocks now plays just once when the block arrives and then stays static, instead of sweeping forever. [log](https://github.com/saropa/saropa-log-capture/blob/main/CHANGELOG.md)

### Fixed

- **ASCII art banner shimmer no longer sweeps forever** — the highlight sweep across grouped ASCII-art log blocks kept animating perpetually even though its CSS was set to a finite iteration count. The log viewport rebuilds its whole visible DOM on every scroll and every incoming log line, which restarted the animation from the beginning each time, so it never settled. The shimmer is now triggered only on a row's first render (latched per row), so it sweeps once on arrival and then holds static through later rebuilds. ([viewer-data-helpers-render.ts](src/ui/viewer/viewer-data-helpers-render.ts), [viewer-styles-ascii-art.ts](src/ui/viewer-styles/viewer-styles-ascii-art.ts))

### Fixed

- **SQL Query History drift status no longer mangles the URL** — the "Drift viewer (from log)" status line joined its parts with no separator, so the banner version ran straight into the base URL (`…8642banner v3.5.1`). The version now reads `… · banner v3.5.1 · unreachable (fetch failed)`. ([viewer-sql-query-history-panel-script.ts](src/ui/viewer-panels/viewer-sql-query-history-panel-script.ts))
- **SQL Query History column headers disable when there's nothing to sort** — Count / SQL / Slow looked clickable even with zero captured queries, where a click silently did nothing. With no data they now render dimmed and inert with a "No SQL queries captured yet — nothing to sort." tooltip, and re-enable as soon as a query is captured. ([viewer-sql-query-history-panel-helpers.ts](src/ui/viewer-panels/viewer-sql-query-history-panel-helpers.ts), [viewer-styles-sql-query-history.ts](src/ui/viewer-styles/viewer-styles-sql-query-history.ts))

---

## [7.17.4]

Each signal in the Signals panel now has a Copy button that puts a paste-ready block on the clipboard, summary signals like "Drift Advisor issues" are clickable to open their detail, your Columns choices stick across reloads, info lines hidden after a stack trace can be revealed again, you can click anywhere on a stack frame to open its source, and right-click submenus stop running off the edge of the panel. [log](https://github.com/saropa/saropa-log-capture/blob/v7.17.4/CHANGELOG.md)

### Added

- **Copy a single signal's full details from the Signals panel** — each signal row (both "Signals in this log" and the cross-session "All signals" list) now has a **📋 Copy** action that puts a paste-ready markdown block on the clipboard: the signal's metadata (kind, severity, occurrences, durations, category, trend, fingerprint), its raw example, and the full untruncated supporting log lines. Drop one signal straight into an analysis engine without copying the whole panel. ([viewer-signal-panel-script-part-b.ts](src/ui/panels/viewer-signal-panel-script-part-b.ts), [viewer-signal-panel-script-part-d.ts](src/ui/panels/viewer-signal-panel-script-part-d.ts))
- **Click "Drift Advisor issues" (and other summary signals) to see their detail** — signals in "Signals in this log" that aren't tied to a single log line (e.g. the Drift Advisor issues summary like "1 error, 2 warnings") had no jump target, so clicking them did nothing and their detail was never shown. These rows are now clickable to expand an inline detail body, with the same pointer/hover affordance as jump-to-line rows. The 📋 Copy action still copies the full block. ([viewer-signal-panel-script-part-b.ts](src/ui/panels/viewer-signal-panel-script-part-b.ts), [viewer-signal-panel-script-part-d.ts](src/ui/panels/viewer-signal-panel-script-part-d.ts), [viewer-styles-signal-sections.ts](src/ui/viewer-styles/viewer-styles-signal-sections.ts))

### Fixed

- **Column choices are now a saved user setting** — the Columns toggles (line numbers, timestamp, session elapsed, tag) reset to their defaults every time the log viewer reloaded. Each column is now a `saropaLogCapture.viewerColumn*` user setting: toggling a column in the viewer saves it as your default (User scope, so it follows you across projects), and every newly opened log opens with your chosen layout. ([viewer-deco-settings.ts](src/ui/viewer-decorations/viewer-deco-settings.ts))
- **Hidden lines after a stack trace no longer vanish without a trace** — device-tier lines default to "warnings & errors only", so an info-level device line (logcat, Awesome Notifications, …) that lands right after an app stack trace was hidden — but its "▶ show hidden" chevron was attached to the trace's last frame row, which doesn't draw a gutter, so the chevron was dropped and the line disappeared with no way to reveal it. Reading the trace then looked like collapsing it had "eaten" unrelated lines. Stack-frame rows now render that reveal chevron (click to show the hidden lines), routed through the same peek control as everywhere else. ([viewer-data-helpers-render-stack.ts](src/ui/viewer/viewer-data-helpers-render-stack.ts))
- **Stack-trace frames are clickable across the whole row** — after the member-first redesign, only the file path (floated hard-right, dimmed) was a link; the prominent member name was plain text, and in a narrow sidebar the path link clipped off-screen, so clicking a frame appeared to do nothing. Clicking anywhere on a stack frame now opens its source at the right line:col (Ctrl/Cmd+click opens beside the current editor). Clicks on the more specific targets a frame can carry — the path link itself, the async-suspension glyph — still route as before, and dragging to select frame text is not hijacked into an open. ([viewer-script-click-handlers.ts](src/ui/viewer/viewer-script-click-handlers.ts))
- **Right-click menu submenus no longer render off-screen** — hovering a submenu like **Copy & Export** could push its flyout partly or wholly past the top or right edge of the panel, making it unusable on short or narrow layouts. Placement was driven by coarse "flip" classes toggled once from the whole menu's position and applied to every flyout uniformly, with no height cap — so a tall flyout simply ran off the screen. Each submenu flyout is now placed against the live viewport from its own trigger position (opens left or right, up or down by available room) and, when taller than the space available, caps its height and scrolls instead of clipping. ([viewer-context-menu.ts](src/ui/viewer-context-menu/viewer-context-menu.ts), [viewer-context-menu-position.ts](src/ui/viewer-context-menu/viewer-context-menu-position.ts), [viewer-styles-context-menu.ts](src/ui/viewer-styles/viewer-styles-context-menu.ts))

### Changed

- **ASCII art banners shimmer twice, then settle** — the highlight sweep across grouped ASCII-art log blocks looped forever, reading as a perpetual "loading" state that competed with live log lines for attention. It now sweeps twice on arrival and goes static. ([viewer-styles-ascii-art.ts](src/ui/viewer-styles/viewer-styles-ascii-art.ts))

<details>
<summary>Maintenance</summary>

- **`scripts/publish.py` interactive prompts no longer corrupt on Windows** — the publish pipeline imported `pyreadline3` (a Windows readline shim) so the version prompt could pre-fill the current version. But merely importing it routes every `input()` through it, and in the VS Code integrated terminal it cannot track the cursor — it mispositioned onto the line above and rendered the cursor mid-word (`Proceed with publish?` → `Pr_ceed`), hiding or garbling every prompt. The shim is now removed entirely; prompts use native `input()`, which writes straight to the terminal so ANSI color and the cursor render correctly. Trade-off: the version-bump prompt no longer pre-fills the current version for in-place editing — it is shown in the prompt and Enter accepts it. ([publish.py](scripts/publish.py), [display.py](scripts/modules/publish/display.py), [orchestrator.py](scripts/modules/publish/orchestrator.py), [version.py](scripts/modules/publish/version.py))
- **`scripts/publish.py` no longer re-offers an already-published version (no-op republish)** — when `package.json` sat at the latest released version (e.g. `7.17.3`) and the CHANGELOG carried a plain `## [Unreleased]` section of new work, the version step suggested that same already-live `7.17.3` instead of bumping. The republish chain then collapsed silently: `package.json` stayed put, the existing `v7.17.3` git tag made the run treat it as a re-publish so `## [Unreleased]` was never stamped, the stores already had `7.17.3` so the propagation check passed instantly, and the success banner printed "v7.17.3 is live!" — yet the new commits never shipped. The suggestion now bumps the patch (`7.17.4`) whenever a plain `## [Unreleased]` section accompanies a `package.json` that is not ahead of the latest released heading; a pinned `## [x.y.z] - Unreleased` heading still wins as the explicit author intent. ([version.py](scripts/modules/publish/version.py))
- **l10n audit stops flagging verified cognates as "untranslated"** — the publish l10n check counts any locale value equal to its English source as a gap. Dozens of short labels (German "Pause"/"Audio"/"System", Spanish "Error"/"local", Italian "Debug"/"file", French "Sources"/"Session") are genuine cognates, loanwords, or abbreviations whose correct rendering IS the English word — but the existing skip lists only cover terms forced English in *every* locale (brands, `SQL`/`ANR`/`OK`), so these surfaced as warnings on every publish. Added a per-locale `VERIFIED_IDENTICAL` allowlist of human-confirmed cognates; the audit now skips them per locale while still flagging real gaps. Also corrected three that were *wrong*, not cognates: French `Volume:` → `Volume :` (French colon spacing), French `Crashes` → `Plantages`, German `Highlights: {0}` → `Hervorhebungen: {0}`. ([l10n_brands.py](scripts/modules/verify/l10n_brands.py), [l10n_bundle_audit.py](scripts/modules/verify/l10n_bundle_audit.py), [bundle.l10n.fr.json](l10n/bundle.l10n.fr.json), [bundle.l10n.de.json](l10n/bundle.l10n.de.json))

</details>

---

## [7.17.3]

Big log files no longer freeze VS Code or peg the CPU while the session list counts up its error and warning badges. [log](https://github.com/saropa/saropa-log-capture/blob/v7.17.3/CHANGELOG.md)

### Fixed

- **High CPU / unresponsive while counting severities on large logs (issue [#30](https://github.com/saropa/saropa-log-capture/issues/30))** — the background scan that fills the session-list error/warning/perf badges classified every line of a log file with a regex bank, and one of those patterns (`\w*(?:Error|Exception)…`) backtracked quadratically on a long unbroken word-run — a base64 blob, hash, or minified-JSON line could take ~2.3 s by itself. On top of that the whole-file scan ran synchronously, so a big report froze the extension host until it finished. The error pattern is now linear (the redundant leading `\w*` is dropped — classifications are unchanged), and the scan yields to the event loop every 2000 lines so even a very large file no longer blocks VS Code. ([level-classifier.ts](src/modules/analysis/level-classifier.ts), [viewer-level-classify.ts](src/ui/viewer-search-filter/viewer-level-classify.ts), [session-severity-counts.ts](src/ui/session/session-severity-counts.ts), [session-severity-scan.ts](src/ui/session/session-severity-scan.ts))

<details>
<summary>Maintenance</summary>

- **Test split: `viewer-context-menu-html.test.ts` back under the 300-line limit** — the toggle-structure, label, tooltip, keyboard-shortcut, and scroll-chrome (`getScrollChromeContextMenuHtml`) cases moved to a sibling [viewer-context-menu-html-toggles.test.ts](src/test/ui/viewer-context-menu-html-toggles.test.ts); the original keeps the structure/copy/hide-submenu cases. Same suite hierarchy, no assertion changes. ([viewer-context-menu-html.test.ts](src/test/ui/viewer-context-menu-html.test.ts))
- **l10n audit no longer flags symbol-only strings as untranslated** — strings with no translatable word (`1 – {0}`, `{0} #`, `Δ #`) are identical to English in every locale because there is nothing to translate, yet the value==English check counted them as gaps on every publish and the translator round-tripped them to Google for identical output. A new `is_no_translatable_content()` predicate shields them the same way `is_brand_only()` shields brands — applied in both the bundle audit's untranslated filter and the translator's force-English path. Removes ~14 recurring false-positive WARN lines (ko/ru/zh-cn/zh-tw now read a true 0 untranslated). ([l10n_brands.py](scripts/modules/verify/l10n_brands.py), [l10n_bundle_audit.py](scripts/modules/verify/l10n_bundle_audit.py), [l10n_translator.py](scripts/modules/verify/l10n_translator.py))
- **Dependabot bumps no longer pile up** — production **minor** bumps were ungrouped, so each (undici, ws, qs, fast-uri…) opened its own PR; while they waited, `package-lock.json` drifted on `main` and the oldest went CONFLICTING. They are now batched with patches into a single `prod-minor-patch` group, and a new auto-merge workflow enqueues patch/minor Dependabot PRs for merge once CI's `build` check passes — majors still stay open for manual review. ([dependabot.yml](.github/dependabot.yml), [dependabot-auto-merge.yml](.github/workflows/dependabot-auto-merge.yml))
- **Cleared all transitive dev-dependency advisories (`npm audit` → 0)** — three moderate advisories sat in build/test tooling (none ship in `dist/extension.js`): `uuid` <11.1.1 (missing buffer bounds check, pulled via `@vscode/vsce` and `nyc`), `serialize-javascript` ≤7.0.4 (CPU-exhaustion DoS, via `mocha`/`@vscode/test-cli`), and `brace-expansion`. Fixed with `overrides` — `uuid` forced to `^11.1.1` (smallest patched release, avoids the `nyc` 15→14 downgrade `npm audit fix --force` wanted) and the stale `serialize-javascript` override raised `^7.0.3`→`^7.0.5` (the lock had pinned the still-vulnerable 7.0.4). ([package.json](package.json))

</details>

---

## [7.17.2]

Tag a log line with a bracket like `[db]` or `[perf:cold start]` and it's routed to that severity level, three more database engines are recognized automatically, and Ctrl+C now copies the selected lines as structured JSON by default. [log](https://github.com/saropa/saropa-log-capture/blob/v7.17.2/CHANGELOG.md)

### Added

- **Log viewer: recognized severity tags from app logs** — prefix a log line with a bracket tag (`[db] bulkPreload wrote 185 rows`, `[perf:cold start] first frame 1840ms`, `[todo:DRIFT-412] backfill`) and the line is routed to that severity level so the existing filter dots group it — including your app's own database-adjacent lines the content heuristic can't detect on its own. Format is `[TAG]` or `[TAG:metadata]`: the tag name is everything before the first colon, the metadata after it stays visible inline. An explicit tag beats keyword guesses (`[db] … failed` stays database), but a real error still wins (`[db] Error: …` stays error). The full vocabulary is published in the README under **Log Tag Vocabulary**; unlisted bracket tags still work as free-form source-tag chips. ([tag-level-dictionary.ts](src/modules/analysis/tag-level-dictionary.ts), [level-classifier.ts](src/modules/analysis/level-classifier.ts), [viewer-level-classify.ts](src/ui/viewer-search-filter/viewer-level-classify.ts))
- **Classifier: 3 more database vendor names auto-detected** — `Sqlite3`, `Prisma`, and `DynamoDB` join the existing Drift/Isar/Sqflite/Hive/Realm/Postgres/MySQL/MongoDB set, so their bracket tags and `Vendor:` prefixes classify as database without an explicit tag. ([level-classifier.ts](src/modules/analysis/level-classifier.ts))

- **Log viewer: Copy to JSON is now the default copy** — `Ctrl+C` (and the new top-most **Copy to JSON** item in the Copy & Export menu) copies the selected lines — or the visible viewport when nothing is selected — as a structured JSON array. Each line becomes one object carrying its 1-based viewer row `line`, ISO `timestamp`, `level`, `category`, parsed `tag`, stream `source`, and `text`; empty fields are omitted so a bare `print()` line stays compact. A raw sub-line text drag-selection is still copied verbatim, since a fragment can't be split into fields. Plain-text copy (`Copy selection`) remains on the right-click menu as **Copy Line** and is rebindable, but loses its default `Ctrl+C` binding to JSON. ([viewer-copy.ts](src/ui/viewer/viewer-copy.ts), [viewer-keybindings.ts](src/ui/viewer/viewer-keybindings.ts), [viewer-context-menu-html.ts](src/ui/viewer-context-menu/viewer-context-menu-html.ts))

<details>
<summary>Maintenance</summary>

- **`scripts/publish.py` summary prompt now defaults to Yes** — `Proceed with publish? [Y/n]` instead of `[y/N]`. The user has already invoked the publish script and read the full irreversible-action summary; a bare Enter should proceed. Ctrl+C / explicit `n` still aborts. ([publish_confirm.py](scripts/modules/publish/publish_confirm.py))

</details>

---

## [7.17.1]

The Logs panel now folds report captures (lint reports, audits) into a collapsible Reports row under each day and shows a banner when newer logs arrive, a dead Crashlytics endpoint no longer leaves the device/OS pane silently empty, and recycled viewer rows no longer ghost faint text from the line that used to sit there. [log](https://github.com/saropa/saropa-log-capture/blob/v7.17.1/CHANGELOG.md)

### Added

- **Logs panel: per-day Reports bucket + newer-log alert** — completes plan [001](plans/history/2026.06/2026.06.02/001_plan-newer-alert-and-reports-grouping.md) by wiring the previously-parked classifier + dormant UI modules into the panel. Each day now renders debug-session captures inline and folds auxiliary captures (Saropa Lint Report, Json Bundle Audit/Translate, Audit Matrix) into a collapsible `Reports (N)` row that respects the new `reportsBucketDefault` setting and a per-day expansion override. A sticky banner above the day list announces logs newer than the panel's last dismiss cursor, with Open / Dismiss buttons; a small blue dot appears on each affected row (gated separately so users can keep one signal without the other). Settings: `reportsKindPatterns`, `reportsBucketDefault`, `newerLogBanner`, `newerLogDot`. Dismiss state persists per workspace and seeds to activation time on first install so pre-existing logs don't carpet-bomb the banner.

### Fixed

- **Crashlytics: per-issue device/OS distribution silently hit a dead endpoint** — the line-analysis panel's `getIssueStats` called `firebasecrashlytics.googleapis.com/.../issues/{id}:getStats`, which is not a public API (always returns a Google frontend HTML 404). The previous `catch { return undefined; }` wrapper turned that 404 into a silently empty pane — the exact pattern bug_008 set out to eliminate. The call is now removed from [analysis-panel.ts](src/ui/analysis/analysis-panel.ts) and the corresponding `issueStatsReady` webview handler is dropped. [crashlytics-stats.ts](src/modules/crashlytics/crashlytics-stats.ts) is now a documented stub that records a diagnostic instead of issuing the dead request. The renderer (`renderApiDistribution`) and `IssueStats` type are preserved so plan 054 can wire the right pane to Play Reporting's `errorCountMetricSet` with `deviceModel` / `apiLevel` dimensions. Closes the last open hole in bug_008.
- **Log viewer: text from a recycled row could ghost through the next row's text** — virtualized rows are swapped in bulk on scroll; Chromium could leave un-invalidated paint inside a slot's bounding box, so the new row's text rendered on top of stale pixels from the prior occupant. Most visible when the slot transitioned between severity colors (e.g. info-blue → database-green): a `DRIFT: Drift debug server disconnected` row showed faint blue characters of the previous row's text bleeding through, fixing itself only on hover. The previous attempt (`transform: translateZ(0)` per-row compositor hint, v7.2.0) shipped in v7.17.0 but was empirically insufficient — Chromium coalesces tiny row layers back into a shared raster. Two layered fixes that don't rely on browser heuristics: (1) `.line, .stack-header` now paints an opaque `var(--vscode-editor-background)` fill rect before the text content so any stale pixels are physically covered (same color as the parent so visually invisible, but the browser DOES rasterize the fill); (2) the viewport renderer in [viewer-data-viewport.ts](src/ui/viewer/viewer-data-viewport.ts) now swaps DOM via a detached `<template>` + `replaceChildren()` + `appendChild(template.content)` instead of `viewportEl.innerHTML = …`, forcing full disposal of prior child nodes so the new rows have no paint-cache lineage with whatever previously occupied the slot. Full attempt history in [plans/history/2026.06/2026.06.02/viewer-row-paint-ghosting-attempts.md](plans/history/2026.06/2026.06.02/viewer-row-paint-ghosting-attempts.md).

---

## [7.17.0]

The footer-filename "Log file" modal now shows the filename, gives a toast when a copy succeeds, and adds Copy filename, Copy relative path, Open beside, Reveal in Explorer view, and Open folder in terminal. Captures with a SAROPA LOG CAPTURE header now show an (i) icon next to the filename — click it to open a structured view of every line in that header. [log](https://github.com/saropa/saropa-log-capture/blob/v7.17.0/CHANGELOG.md)

### Added

- **Session info modal (i) icon next to the filename** — captures that begin with a `=== SAROPA LOG CAPTURE — SESSION START ===` block now expose an info icon in the top-right of the log viewer. Clicking it opens a modal that re-groups the flat header into Session / Launch configuration / Environment / Git / System / Integrations sections, with the launch.json sub-keys indented under their parent. URLs (Git Remote, Drift Advisor) become click-to-open links, and launch-config path values (`program`, `cwd`, `projectRootPath`, `flutterSdkPath`, `dartSdkPath`) become click-to-reveal links into the OS file explorer. Long-press (~0.5s) any row to copy `key: value` to the clipboard. The long "Uncommitted: 77 file(s) — …" line is folded behind a details/summary so the modal stays compact. See [viewer-session-info-modal.ts](src/ui/viewer/viewer-session-info-modal.ts), [viewer-session-info-modal-render.ts](src/ui/viewer/viewer-session-info-modal-render.ts), [viewer-styles-session-info-modal.ts](src/ui/viewer-styles/viewer-styles-session-info-modal.ts).
- **Log file modal: filename shown above the actions** — a dimmed monospace block displays the filename so users can see exactly what the copy buttons will operate on. Read live from the footer at open time to avoid stale values. See [viewer-log-file-modal.ts](src/ui/viewer/viewer-log-file-modal.ts).
- **Log file modal: Copy filename and Copy relative path** — alongside Copy full path (renamed from Copy path). Relative path uses `vscode.workspace.asRelativePath` and falls back to the absolute path when the file is outside any workspace.
- **Log file modal: Open beside, Reveal in Explorer view, Open folder in terminal** — three additional open options for distinct workflows: split-view editing, navigation in the VS Code sidebar tree, and shell tooling at the log's folder.
- **Log file modal: buttons are grouped Copy → divider → Open** — copy actions sit at the top with a dim horizontal rule separating them from the open actions, so the most common action (Open in editor) keeps its primary styling and the copies are obvious.
- **AI activity rows linkify bare relative paths** — tool output like `git add lib/components/x.dart` or `dart format src/foo.dart` previously left the paths as unclickable text because both prior linkifiers required either an absolute root (`d:/…`) or a `:line[:col]` tail. A third pass anchored at known project-folder prefixes (`lib/`, `src/`, `test/`, `bin/`, `tool/`, `scripts/`, `packages/`, `docs/`, `app/`, `pkg/`, `cmd/`, `internal/`, `modules/`, plus build dirs) wraps those paths as `source-link` anchors. Restricted to AI categories so prose mentions of relative paths in regular log lines stay plain. See [source-linker.ts](src/modules/source/source-linker.ts), [log-viewer-provider-batch.ts](src/ui/provider/log-viewer-provider-batch.ts).

### Fixed

- **Ctrl+G in the log viewer opened VS Code's Quick Open `:line` prompt instead of the viewer's Go to Line overlay** — the webview already had `openGotoLine()`, the `ctrl+g` keymap entry, and the overlay HTML / CSS / JS, but VS Code's built-in `workbench.action.gotoLine` keybinding has no `when` clause so it fired globally and preempted the webview keydown handler. The result was a "Open a text editor first to go to a line or an offset" message in Quick Open with no fallback to the viewer's overlay. Added a host command `saropaLogCapture.gotoLineInViewer` that broadcasts a `triggerGotoLine` message, plus a `contributes.keybindings` entry binding `ctrl+g` to that command only when `focusedView == saropaLogCapture.logViewer` or `activeWebviewPanelId == saropaLogCapture.popOutViewer` — so VS Code's default still applies in editors but the viewer's overlay opens inside the log viewer and the pop-out panel. The webview message dispatcher case calls the existing `openGotoLine()`. See [commands-tools.ts](src/commands-tools.ts), [viewer-script-messages.ts](src/ui/viewer/viewer-script-messages.ts), [package.json](package.json).
- **Copy path in the Log file modal had no visible confirmation and could silently no-op** — the host handler used `vscode.window.setStatusBarMessage`, which writes to the status bar at the bottom of VS Code and is easy to miss, and the path was simply not copied at all when `currentFileUri` was unset (the modal still closed because the close runs client-side before the message is posted). Switched all log-file actions to a single dispatcher [viewer-log-file-actions.ts](src/ui/provider/viewer-log-file-actions.ts) that (a) emits `showInformationMessage` toasts naming the exact string copied, (b) emits a warning toast when no log file is active, and (c) emits an error toast if the clipboard write itself fails — so the action can no longer fail silently.
- **AI activity rows rendered their content twice with a fabricated `]` between** — the AI render branch in [viewer-data-helpers-render.ts](src/ui/viewer/viewer-data-helpers-render.ts) extracted the channel label by splitting the row HTML on `]`. But `stripSourceTagPrefix` (on by default) had already stripped the leading `[ai-bash]` bracket above, so the split captured the entire body and synthesized a trailing `]` — visible as `git add foo.dart] git add foo.dart` and similar duplicates. Replaced the brittle split with a regex anchored at offset 0; when no `[LABEL]` is present, no prefix is emitted and the body renders intact. The clipboard was always correct because the underlying text node was single; only the visible HTML duplicated.
- **AI activity rows shifted the line-number column 3 px right of regular log rows** — `.ai-line` carried `border-left: 3px solid` which is in-flow, pushing every digit on AI rows out of column. Swapped for `box-shadow: inset 3px 0 0` (out-of-flow) so the colored rail still paints inside the row's left edge but no longer claims box width. Line numbers across the viewport now sit on the same x regardless of AI category. Per-category color overrides moved to a `--ai-rail-color` custom property. See [viewer-styles-ai.ts](src/ui/viewer-styles/viewer-styles-ai.ts).
- **AI activity rows silently suppressed the severity gutter dot** — the AI branch returned its own `<div>` without `level-bar-*` or `level-*` classes, so a classified AI row (e.g. `dart analyze` flagged as warning) showed no dot or tint. Now applies the same `decoShowBar` / `lineColorsEnabled` guards as the regular branch.
- **Elapsed-time display toggle reset on every reload** — the Options panel checkbox updated the session variable and re-rendered but never told the host, so the workspace `showElapsedTime` setting was read on startup and never written back. Added `setShowElapsed` to the bool-message map and posted from `onDecoOptionChange` guarded on actual state change, so the toggle now persists. See [viewer-workspace-bool-message-map.ts](src/ui/provider/viewer-workspace-bool-message-map.ts), [viewer-deco-settings.ts](src/ui/viewer-decorations/viewer-deco-settings.ts).
- **Elapsed-time values weren't column-aligned** — `.elapsed-time` had `min-width: 50px; display: inline-block` but defaulted to `text-align: left`, so `"17s"`, `"3m 31s"`, `"12m 4s"` left-aligned and the `s` suffix wandered. Added `text-align: right` so all values line up on their right edge. See [viewer-styles-content.ts](src/ui/viewer-styles/viewer-styles-content.ts).

---

## [7.16.2]

The Logs panel kebab menu no longer pops open by itself the first time you open the panel. [log](https://github.com/saropa/saropa-log-capture/blob/v7.16.2/CHANGELOG.md)

### Fixed

- **Logs panel kebab menu no longer appears already-open when the panel is shown** — the popover's visibility was driven by `style="display:none"` plus JS toggling `element.style.display`. The 7.16.1 anchor fix exposed a latent bug where the popover rendered visible the first time the Logs panel opened, even though no code path explicitly opened it (the previous off-screen clipping bug had been hiding it). Switched to class-based visibility in [viewer-styles-session-options.ts](src/ui/viewer-styles/viewer-styles-session-options.ts): the CSS rule defaults `.session-options-menu` to `display: none` and `.session-options-menu.open` switches to `display: flex`. The toggle in [viewer-session-options-menu.ts](src/ui/viewer-panels/viewer-session-options-menu.ts) now flips the `.open` class instead of the inline style, so nothing short of adding `.open` exposes the popover.

---

## [7.16.1]

The Logs panel kebab menu now actually opens when you click it. [log](https://github.com/saropa/saropa-log-capture/blob/v7.16.1/CHANGELOG.md)

### Fixed

- **Logs panel kebab menu now actually opens** — the popover added in 7.16.0 was placed as a sibling of `.session-panel-header` in [viewer-session-panel-html.ts](src/ui/viewer-panels/viewer-session-panel-html.ts) even though its CSS relies on `top: 100%; right: 8px` anchoring against the header's `position: relative`. With no positioned ancestor the popover fell through to the initial containing block, and `.session-panel { overflow: hidden }` then clipped it entirely so clicking the kebab appeared to do nothing. Nested the popover inside `.session-panel-header` so the anchor takes effect and the menu lands directly under the kebab as intended.

---

## [7.16.0]

Opening the Logs side panel now lands on the file currently shown in the viewer instead of leaving the list scrolled wherever it sat last time. Lint-report rule names and code-comment prose no longer get reddened or flagged as warnings by the severity classifier. Logs-list severity badges now agree with the viewer's top-bar counts for the same file. The smart-bookmark prompt that fires during active logging now shows the actual error text in a modal and offers Focus / Copy / Bookmark / Ignore / Dismiss instead of a silent two-button popup. The Logs panel header consolidates its display options behind a kebab menu so the Tags filter no longer disappears off the edge on narrow panels, and the "Latest only" filter now correctly includes logs that exist only once. A new "Export session list to JSON" action saves the current list of sessions to the reports folder.

### Added

- **Logs panel header kebab menu** — the previous toolbar row stacked the date range, four toggles, sort, and the Tags filter inline, so on narrow panels the Tags chip overflowed off-screen and could not be reached. The header now exposes a single kebab button next to refresh/close; clicking it opens a popover containing the date range, every display toggle as a labelled switch, and a separator above a new "Export session list to JSON" action. Menu wiring lives in [viewer-session-options-menu.ts](src/ui/viewer-panels/viewer-session-options-menu.ts); styles in [viewer-styles-session-options.ts](src/ui/viewer-styles/viewer-styles-session-options.ts). Existing button ids are unchanged so `syncToggleButtons`, `bindToggle`, and persistence of `sessionDisplayOptions` keep working without code changes elsewhere.
- **Export session list to JSON** — the kebab menu's new action posts `exportSessionListJson` to the host, which calls the same `buildSessionListPayload` pipeline the panel reads from and writes the result (schema, generation timestamp, project name, total, and full session records) under the reports folder as `YYYYMMDDTHHMMSS_<project>_session-list.json`. Files sort chronologically by filename prefix so repeated exports stack cleanly. Implementation in [viewer-session-list-export.ts](src/ui/provider/viewer-session-list-export.ts); dispatched via the new `exportSessionListJson` case in [viewer-message-handler-session-ui.ts](src/ui/provider/viewer-message-handler-session-ui.ts).

### Changed

- **Smart-bookmark prompt now shows the error and offers five actions** — when active logging hits its first error/warning, the prompt was a two-button "Add bookmark? / Dismiss" toast that hid the actual error text (only line number, message truncated to 80 chars in the title), gave no feedback when "Add bookmark" was clicked (the bookmark landed in workspace state but the user had to discover the Bookmarks side panel themselves), and offered no way to suppress repeat noise short of disabling the feature entirely. [extension-activation-helpers.ts](src/extension-activation-helpers.ts) now opens a VS Code modal (`{modal: true, detail: candidate.lineText}`) with the full error text in the detail field and five named buttons: **Focus** (calls `viewerProvider.scrollToLine` to jump to the row), **Copy** (writes the line to the clipboard + confirmation toast), **Bookmark** (adds to the bookmark store + toast pointing to the panel), **Ignore** (adds the exact line text to a session-scoped `ignoredErrorTexts` set so the prompt never re-fires for that pattern this window lifetime), and **Dismiss** (current behavior — skip for this file only). Both new state buckets live in a `SmartBookmarkSession` object passed into the handler from [extension-activation-handlers.ts](src/extension-activation-handlers.ts); they are intentionally not persisted, so a window reload restores all prompts. Pre-existing guards (one prompt per file, skip if a bookmark already exists on that line) carry over. New l10n keys (`action.focusLine`, `action.copy`, `action.ignoreError`, `msg.errorCopied`, `msg.bookmarkAdded`) added to [strings-a.ts](src/l10n/strings-a.ts).

### Fixed

- **"Latest only" Logs filter now includes singletons and stops duplicating subfolder-shared basenames** — the filter dropped every log that appeared exactly once because `markLatestByName` in [viewer-session-transforms.ts](src/ui/viewer/viewer-session-transforms.ts) gated the `isLatestOfName` flag on `hasDupes > 1`. A name with one entry IS trivially the latest of that name, so the gate was wrong. It also keyed on the FULL stored path rather than the basename, so two files with the same basename living in different subfolders (the rendered row only shows the basename) ended up tracked under different keys and both surfaced as "(latest)" when "Latest only" was on. `markLatestByName` now keys on `getSessionBasename(...)` so the canonical name matches what `renderItem` displays, and the dupe count is recorded separately as `hasNamesakes` so the dim "(latest)" badge still only renders when more than one entry shares the name. The filter checks `isLatestOfName` alone (which always marks the newest), so singletons are kept and the badge stays meaningful.

- **Logs-list severity badges now match the viewer's top-bar counts** — the list panel ran its own parallel quick-scan regex bank (`countSeverities` in [session-severity-counts.ts](src/ui/session/session-severity-counts.ts)) that disagreed with the viewer's authoritative `classifyLevel()` in three documented ways: it missed Flutter "Exception caught by …" banners (E undercounted), missed structural "could not / unable to / failed to …" warnings (W undercounted), and had no debug/database/todo/notice buckets so 200+ debug rows from a typical Drift app collapsed into a gray "other" dot. Producer now calls `classifyLevel()` line-by-line, returning the same 8 buckets the viewer uses. To keep the list paint instant on first launch, the streaming `parseHeader` path no longer scans the body at all — counts arrive from a new deferred worker ([session-severity-scan.ts](src/ui/session/session-severity-scan.ts)) that runs after the list ships, persists results to the central `.session-metadata.json` (V2 schema gated on `debugCount`), and re-posts each row via the existing `sessionListBatch` message so `updateSessionBatchItems` replaces the badge in place. Subsequent launches read the cached counts and skip the scan entirely. The V1 `fwCount` bucket (legacy "framework" pre-classifier) is no longer written; stale V1 sidecars trigger a one-time backfill.

- **Logs panel now opens scrolled to the active file** — opening the panel from the icon bar used to leave the list at whichever scroll offset the previous open had ended at (often the bottom, after a rerender pushed the prior content down), so a user reopening to switch logs had to scroll back up to find the file they were viewing. [viewer-session-panel.ts](src/ui/viewer-panels/viewer-session-panel.ts) now sets a one-shot `pendingScrollOnOpen` flag in `openSessionPanel`, consumed by `renderSessionList` in [viewer-session-panel-rendering.ts](src/ui/viewer-panels/viewer-session-panel-rendering.ts) once the full list arrives: the panel scrolls the row matching the viewer's current file into the center of view, or to the top (where the newest entry sits under the default descending sort) when no file is loaded or no row matches (filtered out, paginated away, or trashed). Matching uses basename so subfolder-disambiguated `data-filename` values still resolve against the workspace-relative `currentFilename`. Subsequent rerenders (filter toggles, pagination, refresh) clear the flag and leave the user's scroll alone.
- **Lint-report rule names no longer classify as errors** — yaml-style rule lists in saropa-lint reports (`    - avoid_print_error`, `    - avoid_catching_generic_exception`, etc.) reddened in the viewer because the strict severity classifier's Dart private-type pattern `_\w*(?:Error|Exception)\b` matched snake_case identifiers — `\w` includes `_`, so the regex engine treated `_print_error` as if it were `_PrintError`. Tightened to `_[A-Z]\w*(?:Error|Exception)\b` in both [level-classifier.ts](src/modules/analysis/level-classifier.ts) and the mirrored webview [viewer-level-classify.ts](src/ui/viewer-search-filter/viewer-level-classify.ts) so the catcher only fires on the actual Dart `_PascalCase` convention.
- **Lowercase "error:" inside prose comments no longer classifies as error** — saropa-lint reports embed yaml comments like `  | # Structural classification error: rule lumps FutureBuilder, StreamBuilder,` and the strict label catcher `\w*(?:error|exception)\s*[:\]!(]/i` reddened them because `/i` made the catcher pick up lowercase `error:` mid-sentence. Dart label and type conventions are always PascalCase, so the main strict branch is now case-sensitive on `Error/Exception`; the bracket form (`[error]`, `[ERROR]`) is split into a separate `/i` regex so explicit log tags still match regardless of case. `Error:`, `TypeError:`, `_TypeError`, `PermissionDeniedException (no OS grant)` continue to classify as error (regression-tested in [level-classifier-special.test.ts](src/test/modules/analysis/level-classifier-special.test.ts)).
- **"cannot see/tell/think" prose no longer classifies as warning** — code comments like `  | # rule's single-method scope cannot see the pair.` warned because `structuralWarnPattern` matched any verb after `cannot`. Real actionable failures always pair the failure phrase with an I/O or system-action verb (open, read, connect, parse, allocate), never with perception or cognition verbs. The pattern now carries a negative lookahead excluding `see|tell|say|imagine|think|know|believe|recall|remember|hear|feel|guess|understand|wait|help`, so `cannot open file` / `could not decode` / `unable to connect` still warn while metaphorical prose stays as info.

---

## [7.15.0]

Log viewer rows no longer ghost their previous color when scrolling, AI activity rows (`[AI Bash]`, `[AI Edit]`, `[AI Read]`) line up in the same column as regular log lines with `+Nms` badges and slow-gap dividers, and the file paths in those AI rows are now clickable. [log](https://github.com/saropa/saropa-log-capture/blob/v7.15.0/CHANGELOG.md)

### Fixed

- **Log Viewer rows no longer ghost previous content as a colored shadow** — recycling a virtualized row slot from one level (e.g. `level-info`, blue) to another (`level-database`, green) via `viewportEl.innerHTML = …` could leave Chromium with un-invalidated paint inside the row's bounding box: the new green text drew correctly but faint blue pixels of the old text stayed visible until a `:hover` repaint cleared the layer. Most visible on the Drift disconnect line because it sat in a slot that had just held an `Application finished.` info line. Added `transform: translateZ(0)` to the `.line, .stack-header` base rule in [viewer-styles-lines.ts](src/ui/viewer-styles/viewer-styles-lines.ts) so every visible row gets its own compositor layer and paints atomically on row recycle. Virtualization keeps ~50 rows live at a time, so the GPU-memory hit is bounded.

### Changed

- **AI activity rows now share the regular line gutter** — Claude Code `[AI Bash]` / `[AI Edit]` / `[AI Read]` lines used to render with no decoration prefix, no elapsed badge, and no slow-gap divider, so they were left-flush against the viewer edge and the rest of the log shifted past them. The AI render branch in [viewer-data-helpers-render.ts](src/ui/viewer/viewer-data-helpers-render.ts) now emits the same `getDecorationPrefix` (line number / timestamp column) + `getElapsedPrefix` (+Nms badge) + `getSlowGapHtml` prefix chain as regular rows, so columns align and a "── 1h gap ──" divider draws when AI tool calls sit inside a long-quiet stretch of log. The `[AI Bash]` etc. text remains as the existing `.ai-prefix` tag — no change to the color scheme. Padding fallback in [viewer-styles-ai.ts](src/ui/viewer-styles/viewer-styles-ai.ts) keeps the 13px gutter only when no `.line-decoration` is present (decoration-off mode).
- **AI Edit/Read file paths are now clickable** — `[AI Edit] d:/src/contacts/lib/views/home/home_tab.dart` rows had no link wrapper because the stack-frame linkifier requires a `:line` tail. Added [`linkifyBarePaths()`](src/modules/source/source-linker.ts) (conservative: only Windows drive letters and POSIX absolute paths, so prose like "see foo.dart" stays plain). Wired into [log-viewer-provider-batch.ts](src/ui/provider/log-viewer-provider-batch.ts) gated on `category.startsWith('ai-')` so regular log lines remain unaffected. Click opens the file at line 1.

<details>
<summary>Maintenance</summary>

- **Publish script no longer inserts an empty `## [Unreleased]` block after stamping a version** — `_stamp_changelog()` in [scripts/modules/publish/version.py](scripts/modules/publish/version.py) used to write `## [Unreleased]\n\n---\n\n` directly above the just-stamped `## [x.y.z]` heading, producing a placeholder section with no entries that polluted the post-publish diff (visible right after the version prompt as `CHANGELOG: [Unreleased] -> [7.14.2]`). Removed the re-insertion — the next publish run's `_ensure_unreleased_section()` already adds `## [Unreleased]` if missing, so the heading only appears once real entries have accumulated.
</details>

---

## [7.14.1]

The magnifier icon in the log viewer title bar now actually opens the search panel, and the search match count shows as a clearer `Showing 4 of 408` badge pill. [log](https://github.com/saropa/saropa-log-capture/blob/v7.14.1/CHANGELOG.md)

### Fixed

- **Magnifier icon in the log viewer title bar now opens the search panel** — the `$(search)` icon in the view title bar (which runs `saropaLogCapture.toggleSearchOverlay`) was setting search state and focusing the input, but the `#search-flyout` container stayed `display:none`, so nothing appeared. Mirrored the existing `closeSearch → closeSearchFlyout` wrapper with an `openSearch → openSearchFlyout` wrapper in [viewer-toolbar-script.ts](src/ui/viewer-toolbar/viewer-toolbar-script.ts), so the flyout is shown whenever `openSearch()` is called from outside the toolbar's own button.

### Changed

- **Search match count is now a colored badge pill** — replaced the dimmed `"4/408"` body text in the search flyout with a `"Showing 4 of 408"` pill using `--vscode-badge-background` / `--vscode-badge-foreground`, so the count reads as a result indicator (matches the badge style used by the toolbar search-count, find panel, and bookmarks). Styling lives in [viewer-styles-search.ts](src/ui/viewer-styles/viewer-styles-search.ts) (`.session-search-match-count`); text format in [viewer-search.ts](src/ui/viewer-search-filter/viewer-search.ts) `updateMatchDisplay()`.

---

## [7.14.0]

Crashlytics now reads crash data from the real public API (Google Play Developer Reporting) instead of a dead endpoint, finds gcloud even when it was never put on PATH, tells you exactly which step failed and how to fix it, and never silently shows "no crashes" when a call actually failed. [log](https://github.com/saropa/saropa-log-capture/blob/v7.14.0/CHANGELOG.md)

### Added

- **Crash stacks are linked to your code (source line + git blame)** — in the in-viewer issue detail, each app stack frame that resolves to a workspace file now shows the **actual source line** and **git blame** for it (`author · when · short-SHA`), streamed in just after the stack renders. New [crash-frame-context.ts](src/modules/crashlytics/crash-frame-context.ts) resolves the file (absolute / workspace-relative / basename search), reads the line, and runs `git blame -L` (capped at 8 frames, 5s each, all best-effort). This is the editor-native angle the Android Studio panel / Play Console can't offer — it has the working tree and git. Plan [054](plans/054_plan-app-quality-insights.md).
- **Crashlytics issues are cached to disk and stay visible offline** — every successful fetch persists the issue list to `.saropa/cache/crashlytics/issues.json`; when a later refresh can't reach the API (offline or error), the viewer shows that cached list instead of going empty, labeled with a stale banner ("Showing cached issues — could not refresh") so it never masquerades as fresh. See [crashlytics-io.ts](src/modules/crashlytics/crashlytics-io.ts) (`readCachedIssues`/`writeCachedIssues`) and [crashlytics-api.ts](src/modules/crashlytics/crashlytics-api.ts).
- **Crashlytics issue detail opens inside the log viewer** — clicking an issue in the Crashlytics sidebar renders its detail (stack trace, device distribution, and the per-frame source line + git blame above) in the viewer's main area, beside the sidebar list — the way opening a session shows in the log viewer, not a separate editor tab. Includes a **Copy as Markdown** button (title, facts, device, stack) for pasting into a bug report or chat. Detail is fetched on click (lazy), so the sidebar list stays fast. See [crashlytics-detail-handler.ts](src/ui/shared/handlers/crashlytics-detail-handler.ts) and [viewer-crashlytics-panel.ts](src/ui/panels/viewer-crashlytics-panel.ts). Part of [plan 054](plans/054_plan-app-quality-insights.md); compact sidebar filters + true metric-set breakdown are the next slices.
- **Jump to code, colorized detail, and polished sidebar** — app stack frames in the issue detail are now **clickable and open the file at the exact line**. The whole detail is colorized (app-vs-framework frames, section headers, device meta, keys), not just the stack. Severity badges are **pill-shaped with icons** (⊗ crash, ⏱ ANR, ⚠ non-fatal) built from theme tokens (so they adapt to any theme), distribution bars are rounded with a subtle gradient, and list typography/spacing was tightened. The in-viewer detail overlay now sits **above** the log toolbar (z-index 200 > the toolbar's 50), so the log's "Log X of Y" / line-count / filename no longer bleed through behind it — and the detail's own header (Back to log + Copy as Markdown) is visible. See [viewer-styles-crashlytics.ts](src/ui/viewer-styles/viewer-styles-crashlytics.ts) and [crash-frame-context.ts](src/modules/crashlytics/crash-frame-context.ts).
- **One-click "Test connection" with a per-step report** — the Crashlytics setup panel now has a Test connection button that runs every prerequisite in order (Google Cloud CLI → sign-in → app config → a live API call) and shows a pass/fail result for each, with a concrete fix on every failure. The full report is also written to the Saropa Log Capture output channel and summarized in a toast. After repeated setup failures you can finally see which link is broken instead of one opaque "Command failed" message. New engine in [crashlytics-connection-check.ts](src/modules/crashlytics/crashlytics-connection-check.ts); wired through [crashlytics-handlers.ts](src/ui/shared/handlers/crashlytics-handlers.ts) and the wizard. Part of [plan 054](plans/054_plan-app-quality-insights.md).
- **Smart stack collapse in the issue detail — your code first, framework noise folded away** — the crash detail now keeps **app frames inline and clickable** and folds each run of more than two consecutive framework frames into a quiet, dashed "⋯ N framework frames" disclosure you expand on demand. No more scrolling past dozens of `package:flutter/…` frames to find the one line in your code. Isolated to Crashlytics (`renderSmartFrameSection`) so the standalone analysis panel keeps its flat full-stack view. See [analysis-frame-render.ts](src/ui/analysis/analysis-frame-render.ts) and [viewer-styles-crashlytics.ts](src/ui/viewer-styles/viewer-styles-crashlytics.ts). Plan [054](plans/054_plan-app-quality-insights.md) (UI #4).
- **"Create issue" button files a crash to GitHub in one click** — the issue detail header now has a **Create issue** button that opens a prefilled GitHub new-issue page: the issue title and the full crash Markdown (facts, device, stack) drop straight into the body, using your workspace's `origin` remote to find the repo. Warns if the remote isn't on GitHub. Reuses the same Markdown the Copy button produces, capped under GitHub's URL-length limit. See [crashlytics-detail-handler.ts](src/ui/shared/handlers/crashlytics-detail-handler.ts). Plan [054](plans/054_plan-app-quality-insights.md) (functionality #4).
- **Regex filter in the Crashlytics sidebar** — the sidebar search box gains a `.*` toggle that matches issue titles as a **regular expression** (case-insensitive) instead of plain substring. An invalid pattern outlines the box in red and leaves the list intact instead of blanking it while you type. See [viewer-crashlytics-interactions-script.ts](src/ui/panels/viewer-crashlytics-interactions-script.ts). Plan [054](plans/054_plan-app-quality-insights.md) (functionality #3, partial — date-range and multi-select still open).
- **Issue detail now reads as a dashboard — stat cards, severity, and a console link** — the in-viewer issue detail gains a row of **stat cards** at the top (severity, events, users, state, version range) so the numbers are visible at a glance instead of living only in the copied Markdown, severity color-coded (crash red / ANR amber / non-fatal muted). The header gets a **"View on Firebase ↗"** link that opens the project's Crashlytics console in the browser (project-level — Play Reporting issue IDs don't map to Firebase per-issue pages, so it links the issues list, not a fabricated deep link). Existing stack / distribution / keys / logs sections stay as labeled panels. See [crashlytics-detail-handler.ts](src/ui/shared/handlers/crashlytics-detail-handler.ts) and [viewer-styles-crashlytics.ts](src/ui/viewer-styles/viewer-styles-crashlytics.ts). Plan [054](plans/054_plan-app-quality-insights.md) Stage 5a.
- **"In your project" panel — this crash may already be fixed** — the issue detail now ties a crash to your actual repository, the angle Android Studio and the Play Console can't reach. For the crash site it shows **recent commits** that touched the file and any **TODO/FIXME/BUG notes** nearby, and — the headline — it parses your workspace `CHANGELOG` and, if releases are listed **after the affected version**, raises a **"⚠ Changed since {version} — may already be fixed"** banner with those newer releases. Honest by construction: if the affected version isn't found in the changelog it says nothing (never implies "nothing changed"); git/changelog/file lookups are all best-effort and stream in after the stack. New pure parser [changelog.ts](src/modules/git/changelog.ts) (unit-tested), orchestration in [project-links.ts](src/modules/git/project-links.ts), rendering in [analysis-project-insights.ts](src/ui/analysis/analysis-project-insights.ts). Plan [054](plans/054_plan-app-quality-insights.md) Stage 5c-1/5c-2.
- **"In your project" also links related pull requests and issues** — when the GitHub CLI (`gh`) is available, the panel adds **PRs that touch the crashing file** and **issues matching the crash's error tokens** (derived from the issue title), each a one-click link to GitHub. Reuses the existing [github-context.ts](src/modules/git/github-context.ts); best-effort, so it shows nothing when `gh` is absent or unauthenticated rather than erroring. Plan [054](plans/054_plan-app-quality-insights.md) Stage 5c-3.
- **Issue detail is now a true multi-pane dashboard** — the detail body became a responsive grid: small data panels (device & aggregate distribution, keys, logs, other threads, "In your project", "Seen in your logs") render as **cards that tile into columns** on a wide overlay, while the stack trace, the stat strip, and the device/thread headers span the full width. It collapses to a single column on a narrow overlay, and panels that stream in (code context, project insights, log correlation) slot into the grid as they arrive. See [viewer-styles-crashlytics.ts](src/ui/viewer-styles/viewer-styles-crashlytics.ts). Plan [054](plans/054_plan-app-quality-insights.md) Stage 5d.
- **Log-viewer ergonomics in the crash stack — line numbers, per-frame copy, app-only filter, context menu** — the stack in the issue detail now borrows the log viewer's conveniences: each frame shows its **`#N` index**, hovering a frame reveals a **copy button** for just that line, an **"App frames only"** toggle hides all framework frames (and the folded framework groups) so you see only your code, and **right-clicking a frame** opens a context menu — Copy frame, Copy file path, Open file, Create issue from frame (path/open shown only when the frame resolves to a source file). Frame rendering stays isolated to the crashlytics smart-stack variant, so the standalone analysis panel is unchanged. See [analysis-frame-render.ts](src/ui/analysis/analysis-frame-render.ts), [viewer-crashlytics-interactions-script.ts](src/ui/panels/viewer-crashlytics-interactions-script.ts), and [viewer-styles-crashlytics.ts](src/ui/viewer-styles/viewer-styles-crashlytics.ts). Plan [054](plans/054_plan-app-quality-insights.md) Stage 5b.
- **"Seen in your captured logs" — a crash linked to your own log history** — opening a crash now searches your saved log sessions for the crash's signature (the exception class name) and, if it finds matches, lists the sessions that already logged it; **clicking a match opens that session's log at the exact line**. This is the angle only this extension has — it holds your captured sessions, so it can say "you already saw this 3 runs ago," which neither Android Studio nor the Play Console can. Best-effort (one representative line per session, recent-first, capped); shows nothing when there's no match. New [crash-signature.ts](src/modules/crashlytics/crash-signature.ts) (unit-tested) + [crash-log-correlation.ts](src/modules/crashlytics/crash-log-correlation.ts) over the existing cross-session [log-search.ts](src/modules/search/log-search.ts). Plan [054](plans/054_plan-app-quality-insights.md) Stage 5c-4.
- **The CHANGELOG can live wherever your project keeps it** — a new `saropaLogCapture.changelogPaths` setting (workspace-relative glob array, default `["CHANGELOG*", "docs/CHANGELOG*"]`, most-canonical first) controls where the "may already be fixed" signal and changelog lookups search, so monorepos and `docs/`-based layouts are no longer stuck with a root-only match. As the groundwork for bringing this intelligence to the whole log viewer, the changelog parser and the project-links orchestrator were promoted out of `modules/crashlytics/` into shared [changelog.ts](src/modules/git/changelog.ts), [changelog-locate.ts](src/modules/git/changelog-locate.ts), and [project-links.ts](src/modules/git/project-links.ts) under `modules/git/`; crashlytics now consumes the shared modules with no behavior change. Plan [055](plans/055_plan-changelog-git-history-in-log-viewer.md) Stage 1.
- **Right-click any source-linked log line for its git history** — a new **View Git History** item on the log line's right-click menu (shown only when the line resolves to a workspace file) opens a popover with **git blame for that exact line** (`hash · author · date` + the commit subject) and the file's **recent commits**. The editor-native angle the raw log can't give: who last touched this code and what changed lately, without leaving the viewer. Best-effort — an untracked file or no-git degrades to a quiet message. New host handler [git-history-handler.ts](src/ui/shared/handlers/git-history-handler.ts) and popover [viewer-git-history-popover-script.ts](src/ui/viewer-context-menu/viewer-git-history-popover-script.ts), reusing the existing blame/log plumbing. Plan [055](plans/055_plan-changelog-git-history-in-log-viewer.md) Stage 2.
- **New "Project state" panel — which code produced this log, and is it maybe already fixed?** — a passive slide-out on the viewer icon bar shows the workspace's **current branch**, **last commit** (`hash · author · date` + subject), and whether the **working tree is dirty**, so a reader knows exactly which code a captured session corresponds to. It also detects the session's **app version** and, if the workspace `CHANGELOG` lists releases **after** that version, raises the same **"⚠ may already be fixed"** banner + newer-release list the crash detail uses — now for any captured session, not just a Crashlytics issue. Passive by construction: with no git and no detectable version it shows a quiet empty state and never nags. New [project-state.ts](src/modules/git/project-state.ts), [project-state-handler.ts](src/ui/shared/handlers/project-state-handler.ts), and [viewer-project-state-panel.ts](src/ui/viewer-panels/viewer-project-state-panel.ts); reuses the shared `modules/git` changelog plumbing from Stage 1. Plan [055](plans/055_plan-changelog-git-history-in-log-viewer.md) Stage 3.
- **Right-click a version on any log line to see what changed since it** — when a log line contains a version token (`1.2.3`, `v2.0.1`, or a date-version like `2026.0301.01`), the right-click menu offers **"What changed since this version?"**, which looks it up in the workspace CHANGELOG and shows the releases listed after it in a popover. Honest by construction: a version that isn't in the changelog says so ("… was not found in the changelog"), and the latest version says "… is the latest release" — neither implies "nothing changed". New host handler [changelog-since-handler.ts](src/ui/shared/handlers/changelog-since-handler.ts) and popover [viewer-changelog-popover-script.ts](src/ui/viewer-context-menu/viewer-changelog-popover-script.ts), built on the Stage 1 shared changelog parser. Plan [055](plans/055_plan-changelog-git-history-in-log-viewer.md) Stage 4.

### Changed

- **Crashlytics consolidated into the log viewer (no separate dashboard tab)** — an earlier unreleased iteration put the full experience in a separate "App Quality Insights" editor tab; it's been folded into the viewer instead. The sidebar is the issue list (each row shows ↗), and clicking a row opens the detail in the viewer's main area beside it. The standalone tab, its command, and the inline-sidebar expander are all gone. See [viewer-crashlytics-panel.ts](src/ui/panels/viewer-crashlytics-panel.ts).
- **The dashboard can no longer hang on an indefinite spinner** — loading now has a 35s backstop timeout and renders a visible error with a **Retry** button (pointing at the output channel for the reason), instead of a silent "Loading…" forever. See [app-quality-insights-panel.ts](src/ui/panels/app-quality-insights-panel.ts).
- **Removed the non-working close/mute buttons from the Crashlytics sidebar; added an "Open dashboard" button** — close/mute can't persist through the read-only Play Developer Reporting API (there's no public write API; Android Studio's "Close issue" uses the dead Firebase endpoint), so the buttons were silent no-ops and are now gone (along with their handler/dispatch/`updateIssueState`). The sidebar header now has a dashboard icon that opens **App Quality Insights** (also in the Command Palette). See [viewer-crashlytics-panel.ts](src/ui/panels/viewer-crashlytics-panel.ts).
- **Crash data now comes from Google Play Developer Reporting, the real public API** — the previous source, `firebasecrashlytics.googleapis.com/.../topIssues:query`, is not a public endpoint (it returns a Google frontend HTML 404), so the Crashlytics issue list never actually worked end-to-end. Reads now go through the Play Developer Reporting `vitals.errors` API (`errorIssues:search` + `errorReports:search`), keyed off the Android package name, mapped back into the existing panel types so the UI is unchanged. New [play-reporting-errors.ts](src/modules/crashlytics/play-reporting-errors.ts) + [play-reporting-mappers.ts](src/modules/crashlytics/play-reporting-mappers.ts); [crashlytics-api.ts](src/modules/crashlytics/crashlytics-api.ts) re-pointed. The client sends the required `X-Goog-User-Project` quota-project header, and the API must be enabled once per project (`gcloud services enable playdeveloperreporting.googleapis.com`). Verified live: `errorIssues:search` returns real issues. Note: issue close/mute is read-only on this API. (bug_008 W3/W5/W6)
- **Sign-in requests the Play reporting scope** — the in-panel "Sign in" now runs `gcloud auth application-default login --scopes=…cloud-platform,…playdeveloperreporting`; without that scope the errors API returns 403 ACCESS_TOKEN_SCOPE_INSUFFICIENT. (bug_008 W4)
- **Crashlytics is enabled by default** — `"crashlytics"` is now in the default `saropaLogCapture.integrations.adapters`. On a project without Firebase/Play config it shows the setup wizard, not an error, so this is safe out of the box. (bug_008 a)

- **Crashlytics setup wizard redesigned to guide instead of dump** — the panel led with raw text ("Install Google Cloud CLI", the winget command, a `gcloud check failed: Command failed…` error box, and an always-open troubleshooting table), which read as an error wall rather than onboarding. It now opens with a one-line purpose, a scannable three-step indicator (Install → Sign in → Project) that fills in green as steps complete, and a plain-language status line per step. The raw diagnostic and step-specific troubleshooting move behind a collapsed "What went wrong?" disclosure, and the billing note + full troubleshooting table sit under collapsed help — so a failed setup reads as guidance, not noise. Same actions and capabilities, reorganized: see [viewer-crashlytics-setup.ts](src/ui/panels/viewer-crashlytics-setup.ts) and [viewer-styles-crashlytics-setup.ts](src/ui/viewer-styles/viewer-styles-crashlytics-setup.ts). First step of [plan 054](plans/054_plan-app-quality-insights.md); the status line is intentionally derived from the step (not the raw gcloud message) so it stays friendly even with the gcloud PATH error tracked in [bug_008](plans/history/2026.06/2026.06.02/bug_008_crashlytics-enable-default-and-gcloud-path.md).

### Fixed

- **Brand names and technical acronyms are kept English in non-English UI** — a string that is entirely a brand (e.g. `Crashlytics`, `Docker`, `Google`, `Saropa`) is now recognized as brand-only and kept verbatim in every locale, instead of being sent to machine translation and risking a transliteration (`サロパリント`, `Pelusas Saropa`). Brand-only detection is derived from the protected-token list rather than a short hardcoded set, and `YouTube` was added to it. Standalone technical acronyms (`SQL`, `ANR`, `OS`, `DB`, `OK`, `TODO`) are likewise forced English in every locale (overwriting any prior translation, so the label stays uniform — no mix of `OK` and `わかりました`) and no longer flagged as untranslated. Abbreviations of real words (`Dev`, `Perf`, `Ver`) and log levels the app already translates (`Error`→`Fehler`) stay translatable. See [l10n_brands.py](scripts/modules/verify/l10n_brands.py); extend `BRAND_TOKENS` or `ACRONYM_ONLY_STRINGS` to add more.

- **Crashlytics / Vitals no longer fail silently** — failed lookups used to `catch { return [] }` (and the Vitals fetch returned `undefined` with no status check), so a missing scope, a disabled API, or a dead endpoint all looked like "no crashes" / "N/A". Every fetch now records a diagnostic on failure, the Vitals panel shows the actual reason (e.g. the exact re-auth command for a missing scope), and the issue list distinguishes a real failure from a genuinely empty result. (bug_008 silent-failure)

- **gcloud is found even when it was never put on PATH, and "not found" is reported as such** — verified on a real machine: `winget install Google.CloudSDK` installs gcloud to `%LOCALAPPDATA%\Google\Cloud SDK\…\bin\gcloud.cmd` but does **not** add it to PATH at all, so a bare `gcloud` call never resolves no matter how many times you restart — and, worse, that "'gcloud' is not recognized" error was mislabeled as a generic "Command failed" (with `shell: true`, a missing command is never a Node `ENOENT`, so the only "not found" branch was dead on Windows). Now a new [gcloud-locator.ts](src/modules/crashlytics/gcloud-locator.ts) probes the known install locations on disk and runs gcloud by absolute path, [runCmd](src/modules/crashlytics/crashlytics-io.ts) quotes paths containing spaces (the `Cloud SDK` directory), and the classifier in [crashlytics-diagnostics.ts](src/modules/crashlytics/crashlytics-diagnostics.ts) recognizes the shell "is not recognized" / "command not found" messages as a missing CLI on both the gcloud check and the token fetch. The 403 classifier now also decodes a missing OAuth scope and a disabled API into copy-pasteable fixes instead of a bare "Permission denied". Tracked in [bug_008](plans/history/2026.06/2026.06.02/bug_008_crashlytics-enable-default-and-gcloud-path.md).

- **"critical CSS" and other "critical &lt;noun&gt;" lines are no longer flagged as errors** — "critical" was a bare error keyword, so ordinary build output like `Injecting critical CSS...` reddened whole rows, inflated the E count, and triggered the error breakpoint. "critical" now signals error only in a severity context — `critical:`, `[critical]`, or `critical error/failure/exception/fault` — so noun phrases ("critical CSS", "critical path", "critical section") classify normally while real `CRITICAL:` logs stay errors. Applied consistently across the viewer level classifier ([level-classifier.ts](src/modules/analysis/level-classifier.ts) + webview mirror [viewer-level-classify.ts](src/ui/viewer-search-filter/viewer-level-classify.ts)), the default `severityKeywords` and "Fatal" highlight rule, the session-history error count, and the error-breakpoint detector. Users who explicitly list "critical" in `saropaLogCapture.severityKeywords.error` keep the bare-word match.

<details>
<summary>Maintenance</summary>

**Localization**

- **The log viewer webview is now fully localizable** — completed the plan 053 sweep so every remaining user-facing string in the viewer (decoration tooltips, render labels, context menus, modals, popovers, and the performance / recurring / session-comparison / line-analysis / SQL-history panels) is routed through the l10n pipeline. 353 English source keys were added across [strings-webview-b.ts](src/l10n/strings-webview-b.ts) (client `vt()`) and [strings-viewer-d.ts](src/l10n/strings-viewer-d.ts) (host `t()`), both wired into the merged `strings` map and the `__VT` bridge in [l10n.ts](src/l10n.ts). No visible change in English; non-English VS Code picks up machine-translated strings at the next publish (translation is automated via `scripts/translate_l10n.py`; bundles are not hand-edited). Plan [053](plans/history/2026.05/2026.05.23/053_plan-webview-localization-sweep.md).

</details>

---

## [7.13.2]

Flutter stack traces with elided-frames summaries no longer fragment into multiple collapsible groups, and an error incident's left rail and severity spine run continuously through the whole stack trace instead of breaking at the banner. [log](https://github.com/saropa/saropa-log-capture/blob/v7.13.2/CHANGELOG.md)

### Fixed

- **Flutter stack traces with frame-elision summaries now collapse as one group** — Flutter substitutes a single `...     Normal element mounting (N frames)` row for a long run of collapsed framework frames. That summary row was not recognized as a stack frame, so it hit the group-close path in [viewer-data-add.ts](src/ui/viewer/viewer-data-add.ts) and shattered one logical trace into a separate collapsible group at every elision — a single `#0`–`#262` trace appeared as five `▸` toggles, one per `(N frames)` line. A new `isElidedFramesSummary()` classifier in [viewer-script.ts](src/ui/viewer/viewer-script.ts) folds the summary INTO the open group (the same way `<asynchronous suspension>` markers already do), forcing the framework flag so preview-collapse hides it with the rest of the framework noise; it stays a visible, collapsible row inside the one unified group. Guarded so a stray summary with no open trace stays a normal line. See [viewer-data-add-stack-ingest.ts](src/ui/viewer/viewer-data-add-stack-ingest.ts) and tests in [viewer-stack-elided-summary.test.ts](src/test/ui/viewer-stack-elided-summary.test.ts).
- **Error incident's left rail and severity spine are now continuous and aligned through the stack trace** — the 3px red grouping rail and the faint severity connector stripe stopped at the Flutter exception's banner text and did not continue across the stack frames, so the left edge of an error block looked broken (rail present on the prose rows, gone on the frames; the connector stripe also sat in a different column, ~8px to the right). Error-level stack rows now carry the same rail ([viewer-styles-flutter-banner.ts](src/ui/viewer-styles/viewer-styles-flutter-banner.ts)), and the connector stripe is pulled under the rail (`0.30em`) for the whole incident. This also repairs a latent specificity bug where the banner's own connector override was silently outranked by the default chain connector and rendered detached at `0.89em`. `border-left` only on the stack rows (no `padding-left`) so the decoration column stays put. Side effect: a standalone error stack (not just Flutter exceptions) also gains the continuous red rail.

## [7.13.1]

The log viewer is now localizable into the 10 packaged locales, render-tree descendant dumps collapse into a single group, Dart app stack frames read symbol-first (so the method name lines up at the left instead of getting shoved across the screen by long paths), `dart:async` SDK frames fold into the stack trace they belong to, and every printed copy of a Flutter exception (not just the stderr one) now groups. [log](https://github.com/saropa/saropa-log-capture/blob/v7.13.1/CHANGELOG.md)

### Added

- **Render-tree descendant dumps are now a collapsible group** — a Flutter layout exception appends `This RenderObject had the following descendants (showing up to depth N):` followed by 15–40 indented `child…` rows (in a real 4,793-line log: 30 such dumps, 438 child rows). These had no handling and rendered as plain lines, burying the actual error. A new detector [viewer-data-add-tree-ingest.ts](src/ui/viewer/viewer-data-add-tree-ingest.ts) folds the dump into one collapsible group, reusing the existing stack-group machinery (`stack-header` + `stack-frame` types) so the chevron, `toggleStackGroup`, preview mode, and height/visibility calc all apply with no new render code. A `treeGroup` flag re-words the header tooltip to "Render tree" / "nodes" in [viewer-data-helpers-render-stack.ts](src/ui/viewer/viewer-data-helpers-render-stack.ts). Child indentation is preserved (it carries the hierarchy). It owns a separate `activeTreeHeader` lifecycle so stack dedup never silently hides a tree, reset on marker / clear / trim. Consumed before the banner classifier (like stack frames), so the tree forms its own group inside the exception band. See [plan 052](bugs/052_plan-flutter-exception-grouping.md).

### Fixed

- **Error "Related Lines" in signal reports now read like every other signal type** — the `err::` branch rendered its own per-fingerprint-group markup instead of the shared list convention the other six branches (warnings, network, memory, permissions, slow ops, classified) use. As a result it showed no single "N error(s)" aggregate, leaked the raw excerpt into the group summary even when a fuller log line was available, and never rendered the "...and N more" overflow notice. It now flattens to one row per error through the same `wrapList` + `itemRow` path — aggregate count, log-line-preferred text, a category badge per row, and the overflow cap — so the error section is consistent with the rest. Fingerprint collapse was evaluated but is incompatible with the section's overflow contract (the cap counts occurrences, not distinct fingerprints), and the cap already bounds a flood of repeats. See [bug 006](bugs/bug_006_signal-report-error-related-rendering.md). ([signal-report-related.ts](src/ui/signals/signal-report-related.ts))
- **`dart:async` SDK frames no longer leak out of their stack trace** — Dart's `stack_trace` package emits SDK frames as a bare library padded out to align the member, e.g. `dart:async                Future.timeout.<fn>`. With no `/path line:col`, these matched none of the stack-frame patterns ([isStackFrameText](src/ui/viewer/viewer-script.ts) / [isStackFrameLine](src/modules/analysis/stack-parser.ts)), so they fell through to the normal-line path: rendered with the raw padding intact (library on the left, member shoved far right by `white-space: pre-wrap`) **and** closing the open stack group, fragmenting every trace. A dedicated `dart:<lib>  member` branch now classifies them as real frames so they fold into the trace like every other frame. When shown, [formatFrameMemberFirst](src/ui/viewer/viewer-data-helpers-core.ts) leads with the member and moves the library to a muted, right-aligned `.frame-lib-src` source tag (padding trimmed); `rawText` is kept verbatim so copy/search are unaffected.
- **App stack frames now read symbol-first, not path-first** — Dart's `stack_trace` `Trace.toString()` emits app frames as `<path> <line:col>  <Member>` and right-pads the path column to the width of the *longest* frame, so a long `./lib/...` path (e.g. the 70-char `activity_drift_extensions_io.dart`) shoved the meaningful method name far to the right behind a wall of spaces under `white-space: pre`. The viewer rendered them verbatim — only `dart:` SDK frames had been reordered. [formatFrameMemberFirst](src/ui/viewer/viewer-data-helpers-core.ts) now also reorders **app** frames: the member leads and the file location moves to the muted, right-aligned `.frame-lib-src` source tag, collapsing the alignment gap. Unlike `dart:` frames (rebuilt from plain text), app frames arrive already linkified as `<a class="source-link">…</a>` carrying click-to-open and Ctrl+click-to-filter data, so the link element is lifted **intact** via an HTML regex rather than rebuilt — a plain-text rebuild would `escapeHtml` the link away and silently kill both affordances. Display-only: `plainFrame`, `rawText`, and the repeat-collapse comparison still use the original html, so dedup, search, and raw copy are unchanged.
- **Every printed copy of a Flutter exception now groups, not just the stderr one** — a single layout exception is logged once per sink in slightly different shapes, but the banner detector in [viewer-data-add-flutter-banner.ts](src/ui/viewer/viewer-data-add-flutter-banner.ts) only matched the stderr form `════ Exception caught by …`. The console / wrapped copies — `══╡ EXCEPTION CAUGHT BY … ╞══`, `FlutterErrorDetails (══╡ …`, and `Potential Null Check Operator Error Detected: ══╡ …` — were never grouped (real log: 16 of 46 headers grouped, 30 left ungrouped). The console shapes break a pure-`═` run after only two chars with the corner glyphs `╡` (U+2561) / `╞` (U+255E), so the old `/═{4,}\s+/` anchor never fit them. Open regex broadened to `/[═╡╞]{2,}\s*Exception caught by\b/i`: the phrase is the discriminator, the leading box-run guards against prose. ANSI is already converted to `<span>` upstream and stripped via `slp.msg`/`stripTags`, so the existing close-rule detection fires for every copy. Each copy now gets its own band. See [plan 052](bugs/052_plan-flutter-exception-grouping.md).

<details>
<summary>Maintenance</summary>

- **Publish-pipeline build prompts now default to Yes on ENTER** — the reversible build-flow confirmations let you tap ENTER through a routine build instead of typing `y`: "Continue with dirty working tree?" ([checks_git.py](scripts/modules/publish/checks_git.py)), "Install via CLI now?" and "Open build report?" ([install.py](scripts/modules/publish/install.py)), and the post-publish "Open Marketplace in browser?" ([report.py](scripts/modules/publish/report.py)). The irreversible **"Proceed with publish?"** gate ([publish_confirm.py](scripts/modules/publish/publish_confirm.py)) deliberately stays default-No — a marketplace publish can't be undone, so bare ENTERs build and stop at that gate rather than shipping by accident.
- **Publish pipeline no longer locks up at Step 9 during l10n translation** — Step 9 machine-translates ~468 newly-synced strings × 10 locales through `deep-translator`'s free Google endpoint, which calls `requests.get()` with **no timeout** ([deep_translator/google.py](https://github.com/nidhaloff/deep-translator)). Once Google throttled the burst, a single call blocked forever and the release hung — the on-screen `… No translation was found using the current translator. Try another translator?` was the text of a caught `TranslationNotFound` exception, not an interactive prompt. [l10n_translator.py](scripts/modules/verify/l10n_translator.py) now (1) bounds every call via the process-wide socket default timeout (8s, set around the translate loop and restored after), (2) retries a transient failure once with backoff so a throttle blip doesn't permanently lose a string to English, and (3) trips a circuit breaker after 5 consecutive **network** failures (a brand-validation reject proves the endpoint is healthy and does not count) — keeping English for the rest and signaling the caller to stop the remaining locales, since throttling is per-IP. Both callers ([checks_build.py](scripts/modules/publish/checks_build.py), [translate_l10n.py](scripts/translate_l10n.py)) now unpack the new `aborted` flag and stop early with a "re-run `scripts/translate_l10n.py` later" notice instead of hanging the release. The translate step also streams a per-locale `done/total` counter (`translate_locale` gained an `on_progress` callback): right after a big English-bundle sync the backlog is ~4,680 calls that previously ran **silently** for 15+ minutes — visually indistinguishable from the hang — so Step 9 now shows continuous forward motion, and the run is announced up front as a network step that can take several minutes.
- **Decoration-styles regression test no longer false-fails** — the "no standalone `.deco-counter` color rule" guard in [viewer-muted-decorations.test.ts](src/test/ui/viewer-muted-decorations.test.ts) used a plain substring check that also matched the legitimate `.deco-counter-row` collapse-affordance class, so it tripped even though the bare `.deco-counter { … }` color rule it forbids is genuinely gone. Tightened to a word-boundary regex (`/\.deco-counter(?![\w-])/`) that ignores `-row` and any other suffix. See [bug 007](bugs/bug_007_deco-counter-test-substring-collision.md).
- **Log viewer is now localizable** — the webview was English-only; neither existing l10n pipeline (`package.nls*` for `package.json`, `vscode.l10n.t()` for extension-host UI) reached it. A new bridge ([viewer-l10n-inject.ts](src/ui/provider/viewer-l10n-inject.ts)) resolves webview string keys host-side and ships them into the iframe as `__VT` with a client-side `vt(key, …args)` helper, so render-time strings (built where `vscode.l10n.t()` can't run) become localizable; host-assembled HTML uses `t()` directly. The whole visible UI now routes through this — toolbar, filter drawer, search (flyout + in-log bar), actions menu, icon bar, body banners, and every side panel (Logs/session, Find, Bookmarks, Signals, Trash, Collections, Crashlytics, About chrome, SQL history, Options), plus the replay bar, run-nav, and stack/tree tooltips. English source strings only (brand/marketing copy and proper nouns stay English by design); the publish pipeline machine-translates the bundles across all 10 locales, and missing keys fall back to English (no behavior change today). Tracked sweep in [plan 053](plans/history/2026.05/2026.05.23/053_plan-webview-localization-sweep.md).
</details>

---

## [7.13.0]

Info lines near an error no longer get tinted red, Dart `stack_trace` `Trace.toString()` output is recognized as frames (with click-to-open paths), Shift+arrow keys extend the row selection in the log viewer, and Ctrl+clicking a folder segment in a path link filters the log to lines containing that prefix. [log](https://github.com/saropa/saropa-log-capture/blob/v7.13.0/CHANGELOG.md)

### Fixed

- **Recent-error proximity tint removed — info lines near an error no longer go red** — every info-level row arriving within 2 s after a real error was promoted to `level='error'` + `recentErrorContext=true` by [viewer-data-add.ts:138-149](src/ui/viewer/viewer-data-add.ts#L138-L149) (the "Recent-error context" band). In dense logs this dragged unrelated subsequent primary log lines red — e.g. `[log] Event Badge Displayed: count: 5` arriving milliseconds after `[log] Repeated log #1 (... PermissionDeniedException ...)` got tinted, and its own stack frames inherited via `previousLineLevel` so the whole block went red. The 2 s window is a time-based heuristic with no way to distinguish "trailing context for the error" from "next unrelated app event" — wrong by construction in dense logs. The promotion block is now removed; errors come solely from their own classifier signal. Stack groups still render together via stack-parser (the correct continuation signal). The `proximityInheritAnchor()` helper in [viewer-data-add-context-helpers.ts](src/ui/viewer/viewer-data-add-context-helpers.ts) is deleted; `previousLineLevel()` stays (still used for stack-header level inheritance, no longer needs to skip recent-error rows because nothing produces them anymore). Defensive `row.recentErrorContext` filters in the root-cause-hints collectors and renderer styling are left as no-ops — harmless dead branches in case a future feature wants the flag back.
- **Dart `stack_trace` Trace format now detected as frames** — `Trace.toString()` output emits `<indent><path> LINE:COL  Member.name` with a SPACE between path and `line:col` (e.g. `      ./lib/foo.dart 273:9  Foo.bar`). The existing indented-frame regex in [stack-parser.ts:262](src/modules/analysis/stack-parser.ts#L262) required a colon directly attached to the filename (`file.dart:273`), so contacts-app `dart:developer log(stackTrace:)` traces fell through to normal-line rendering — every six-frame trace took six full rows of vertical space with no grouping or collapse. A new regex `^\s+\S*\/\S+\.\S+\s+\d+:\d+\s+\S` matches the space-separated variant; mirrored into the webview's `isStackFrameText()` in [viewer-script.ts:149](src/ui/viewer/viewer-script.ts#L149). Requiring `/` in the path avoids matching prose with `file.ext 1:2` shapes; requiring trailing `\s+\S` (member name) keeps audit-report lines from being misread as frames.
- **Trace-format paths now click-to-open in the editor** — the linkifier regex in [source-linker.ts:26-27](src/modules/source/source-linker.ts#L26-L27) only matched colon-attached `file.ext:line`, so `Trace.toString()` paths (`./lib/foo.dart 273:9`) rendered as plain text and the click-to-source affordance went dark for every Dart `dart:developer log(stackTrace:)` trace — even after frame detection started routing them through stack-grouping. The combined regex now handles both shapes in a single alternation: branch A `file.ext:line[:col]` (capture groups 1..4) and branch B `file.ext LINE:COL` with mandatory column (capture groups 5..8). One replace pass keeps the second pattern from re-wrapping HTML the first pass already turned into `<a>` tags. Branch B requires `/` in the path and the column digit so prose like `see foo.dart 42 description` stays untouched.
- **Single-frame Trace stacks now render as regular lines** — `Trace.toString()` output where a `[log]` message has a one-frame stack (common pattern in the contacts app `IsarDriftRowCountAudit` rows: one `[log]` line followed by one frame) was synthesizing a `.stack-header` from that lone frame. The `.stack-header` HTML composition (`hdrDeco + hdrQb + html.trim() + dup`) is missing the `elapsed / badge / catBadge` prefix elements that regular `.line` rows carry, so the synthesized header sat visually LEFT of the parent log message — and there was no chevron-able content to make the stack-header treatment worthwhile in the first place. The dispatch in [viewer-data-helpers-render.ts:160](src/ui/viewer/viewer-data-helpers-render.ts#L160) now gates `renderStackHeader` on `frameCount > 1`; single-frame "traces" fall through to the regular `.line` render path so they share the same prefix composition and column alignment as the message above. Multi-frame traces are unchanged — still collapsible with chevron by default.

### Added

- **Keyboard line-selection extension in the log viewer** — `Shift+Down` / `Shift+Up` now extend the row selection by one line in the direction pressed; `Shift+PageDown` / `Shift+PageUp` extend by a viewport; `Ctrl+Shift+End` / `Ctrl+Shift+Home` extend to the last / first line. When the cursor crosses the anchor (the originating click), direction inverts naturally — pressing `Shift+Up` on a downward selection shrinks it line-by-line, and continuing past the anchor extends upward. Because the viewer has no caret, the first `Shift+Arrow` with no active selection uses the **last plain (non-shift) click** as its anchor, falling back to the first visible line if no click has happened yet. Helpers live in the new [viewer-selection-keyboard.ts](src/ui/viewer/viewer-selection-keyboard.ts); the cursor (`selectionEnd`) is what the keyboard moves while the anchor (`selectionStart`) stays put — [updateSelectionHighlight()](src/ui/viewer/viewer-copy.ts#L236) already uses `min/max` so no extra bookkeeping is needed when the cursor passes through the anchor. Filtered / hidden / zero-height rows are skipped so motion matches what the eye sees. All six new bindings are user-rebindable through the existing viewer keybindings config; defaults match VS Code conventions for selection extension. Native drag-select is cleared on first keyboard extension so the two selection systems never render simultaneously.
- **Ctrl+click a folder/file segment in a path link to filter the log** — every `<a class="source-link">` now contains per-segment `<span class="source-link-seg" data-prefix="...">` spans for each folder and the filename, each carrying the cumulative prefix up to that point ([source-linker.ts:`buildPathSegmentSpans`](src/modules/source/source-linker.ts)). Hold Ctrl/Cmd and hover over a segment: CSS `body.ctrl-held .source-link-seg:has(~ .source-link-seg:hover), body.ctrl-held .source-link-seg:hover` lights up the hovered segment plus every earlier sibling — so hovering `drift_middleware/` highlights `./lib/database/drift_middleware/` (exactly the prefix the click will filter on). Ctrl+click on a segment drops that prefix into the search input, forces filter mode on, and re-runs the search via the new [`filterToPathPrefix()`](src/ui/viewer-search-filter/viewer-search.ts) helper so the log collapses to lines containing it. Plain click anywhere on the link (including on a segment) still opens the file at line:col — only Ctrl+click on a segment routes to filter. Leading `./` and `/` are merged into the first real folder so the first segment is `./lib/`, not a useless bare `./`. Ctrl-tracking via `body.ctrl-held` toggled on keydown/keyup/blur ([viewer-script-keyboard.ts](src/ui/viewer/viewer-script-keyboard.ts)).

---

## [7.12.1]

Version bump for publication only — no user-facing changes. [log](https://github.com/saropa/saropa-log-capture/blob/v7.12.1/CHANGELOG.md)

---

## [7.12.0]

Right-click → **Columns** is now its own submenu where you can flip the per-line column chips on and off (including a new toggle for the source-tag chip like `flutter` or `HWUI`), the activity-bar badge stops piling up while the Logs view is already open, clicking a row in the Signals sidebar actually opens the right session and scrolls to the line, collapsed stack gaps now caption themselves so a hidden run of frames doesn't look like a mystery 8 px gap, and the scrollbar minimap finally shows the same colors as the gutter next to it. [log](https://github.com/saropa/saropa-log-capture/blob/v7.12.0/CHANGELOG.md)

### Added
- **Collapse numeric variants — fold consecutive lines that differ only by digit runs** — source apps that already deduped a flood ("Repeated log #1", "Repeated log #2", …) defeat every downstream byte-equal dedup: `FloodGuard` (capture-side) and `applyCompressDedupModes` / `compressLinesMode` both treat `#1` and `#2` as distinct strings, so 29 "Repeated log #N (…)" lines render as 29 rows. A new viewer option `Collapse numeric variants (digit-only differences)` in the Options panel, **on by default**, runs the existing consecutive dedup pass with a digit-normalized key (`text.replace(/\d+/g, '<n>')`), so runs whose non-digit characters all match collapse into one survivor row with a `▼ duplicate` chevron and `×N` count badge. Reuses `compressDupHidden` / `compressDupCount` / `compressDupHiddenIndices` so the existing peek-chevron handler in [viewer-peek-chevron.ts](src/ui/viewer/viewer-peek-chevron.ts) expands the fold without modification. Suppressed automatically when either explicit compress mode is on — the user's strict opt-in wins. Lines like `"Loaded 42 items"` and `"Loaded 43 items"` will fold; that's the feature.
- **Columns submenu in the right-click menu** — a new top-level **Columns** submenu (above **Layout**) gathers the per-line column toggles in fixed order: Line numbers, Timestamp, Session elapsed, and a new **Tag** toggle for the parsed source-tag chip (e.g. `flutter`, `HWUI`). Layout now keeps only whole-row treatment toggles (wrap, spacing, line height, compression). The Tag toggle is independent of the structured-prefix-strip option — turning the chip off does not put the bracket prefix back into the message text.
- **SQL Query History — `Cumulative across logs` toggle (DB_17 Step 1)** — the panel previously emptied itself whenever the active log lacked Drift output, even though the workspace had captured many SQL fingerprints across other logs. A new toolbar checkbox layers in fingerprints aggregated from every other sidebar log's persisted `driftSqlFingerprintSummary` (the data is already on disk from `DB_10`), so you can see whether a fingerprint was ever observed without hunting through logs one by one. The active log is excluded from the baseline so its live rollup is never double-counted; the toggle hides itself on empty workspaces and persists per-webview via `vscodeApi.setState`. Cross-log rows route their jump button through a new `sqlHistoryCrossLogJump` host message that loads the source log first and then scrolls. **Limitation:** Step 1 cumulative-only rows show the bare fingerprint string instead of readable SQL text — Step 2 will bump the persisted schema to v2 with a sample preview so cross-log rows look as readable as live rows.

### Changed
- **Escape clears any active selection in the log viewer** — the `escape` keybinding closed any open modal/panel (peek, goto-line, search, find, options, session) but left both flavors of row selection sitting on screen: the viewer's click-selection state (`selectionStart`/`selectionEnd` + `.selected` class set by shift-click in `viewer-copy.ts`) AND the browser's native text selection from drag-select (`window.getSelection()`). The escape handler in [viewer-script-keyboard.ts:61-72](src/ui/viewer/viewer-script-keyboard.ts#L61-L72) now also calls `clearSelection()` and `getSelection().removeAllRanges()` after the modal-close cascade, so a single escape press leaves a clean viewport regardless of which selection mechanism was active.
- **Stack-header expander chevrons dimmed at rest** — the inline `▶` / `▼` chevron on collapsible stack headers used the full `--vscode-descriptionForeground` color, which made it compete with the header text. The chevron now sits at `opacity: 0.5` so it reads as a quiet hint while idle, and returns to full opacity on hover so the affordance is still discoverable.
- **All three viewer-divider variants relabeled to a one-word `hidden` chip aligned to the tag column** — the filter-hidden-gap pill (was `─── N hidden lines · show ───`), the peek-collapse brackets that wrap an expanded peek (was `─── hide N revealed · collapse ───` and `─── hide N revealed (above) · collapse ───`), and the preview-mode notice at the cutoff of a partially-expanded stack (was `─── N more stack frames hidden · show all ───`) all collapse to the same one-word `hidden` chip, with a chevron prefix encoding state (`▶ hidden` collapsed = click expands; `▼ hidden` expanded = click collapses), left-aligned to land in the same x-column as the structured-prefix tag chip (`flutter`, `MediaSessionCompat`, …) on real log rows. The long-form descriptions ("N hidden lines · show", "hide N revealed (above)", "N more stack frames hidden") move to the title tooltip + aria-label so screen readers still get the full count and position. Alignment uses a new CSS custom property `--deco-tag-position-px`, MEASURED at render time by `measureTagColumnPosition()` (in `viewer-decorations.ts`) reading the bounding rect of a real `.deco-parsed-tag` element vs its containing `.line`. Earlier attempts estimated the offset by summing the per-part widths in `applyDecorationLayoutWidth` — every variant was systematically wrong because those estimates are in parent em while the prefix renders in 0.85em-of-parent, AND the underlying parts use less than their generous reservations, so the chip landed in a different x-column from real tags every time. The DOM-measured value is pixel-perfect and follows the user's font-zoom without recompute. Measurement runs after each `renderViewport` DOM write, so the variable is always in sync with what's actually on screen; a cached `lastMeasuredTagLeftPx` guards against jitter when no tag is currently visible.
- **Viewer-divider rows no longer claim layout height — every inter-row gap is now uniform** — the `.viewer-divider` (filter-hidden gap pill, peek-collapse bracket, preview-frames notice) used to be a real layout row at `max(8px, calc(0.7 * 1em * --log-line-height))`, so pairs of visible rows that had hidden lines between them sat ~0.7em farther apart than pairs that didn't. That visual stretch read as inconsistent spacing — most obvious where two truly adjacent rows (no hidden lines between them) had a smaller gap than every neighboring pair on screen. The divider now has `height: 0` with `overflow: visible` and the visible label paints as a `position: absolute; top: 0; transform: translateY(-50%)` overlay centred on the row boundary. Inter-row gap is now exclusively a function of `--log-line-height` (the same value the font-zoom hot-key + scroll wheel adjusts), so all gaps remain identical when the user resizes. The chain stripe still bridges through divider siblings via the same `:has(+ .level-bar-X)` selector, and click routing through `data-divider-action` is unchanged (the zero-height parent sets `pointer-events: none`, the label sets `pointer-events: auto`).
- **One vocabulary for every "click to expand/collapse hidden content" affordance — chevron + one word in the tag column** — three affordances used to look completely different: the stack-header expander was an inline `▶ / ▼` chevron buried in the row's text; the dedup-fold survivor showed a trailing `×N` / `×N hide` pill at the END of the row; and the filter-hidden gap appeared as a centred `─── N hidden lines · show ───` pill on its own row. Three states (collapsed, expanded, partial), three colors, three positions, three label conventions — the user couldn't predict from looking what a click would do. All three now use the same chip — a chevron prefix (`▶` collapsed, `▼` expanded) followed by exactly one word: `stack` for stack-trace expansion (and the preview-mode "more frames" divider, since both scope to a stack group), `hidden` for filter-suppressed lines, `duplicate` for dedup-fold survivors. All three sit at the same x-column as the parsed source tag (`flutter`, `MediaSessionCompat`, …) on real log rows, so they read as one repeating column-aligned marker. The chevron direction is the single source of truth for state; the count and per-reason breakdown move to the title tooltip + aria-label so screen readers still get the full info.
- **All expand/collapse affordances on regular log rows collapse into a single clickable line-number column — including stack-trace toggles** — the in-tag-slot dedup chip + the between-row `▶ hidden` / `▼ hidden` / `▶ stack` divider rows + the inline `▶ stack` chip on stack-header rows all had visual problems: between-row labels overlapped tag chips of adjacent rows, multiple dividers at the same row pair stacked on top of each other, and the inline stack-header chevron sat in the middle of the row with no consistent column position. Retired all of them. Filter-hidden gaps, dedup-fold survivors, expanded peeks, AND stack-trace toggles now surface ONE way: a small `▶` (collapsed) or `▼` (expanded) chevron rendered immediately right of the line-number digits in the decoration prefix. The whole `.deco-counter-row` wrapper (line-number digits + chevron) is the click target — aim at the number OR the chevron, same action. Click routing is keyed off `data-affordance-kind` (dedup / stack / gap / peek) so all four reuse the same delegate path in [viewer-peek-chevron.ts:53](src/ui/viewer/viewer-peek-chevron.ts#L53). A per-render pre-pass (`computeRowAffordances` in [viewer-data-divider.ts](src/ui/viewer/viewer-data-divider.ts)) stamps `_hiddenAfter`, `_triggeredPeekKey`, and `_stackToggleGid` so the chevron knows what to dispatch without each row scanning forward. **Column alignment**: rows without any affordance still render an empty `.deco-chevron` spacer of the same fixed width as the glyph, so the numeric column stays straight regardless of whether a chevron is active. **Stack-trace toggle ownership**: when a row's immediate next visible neighbor is a multi-frame stack-header, the chevron lives on the row above (the log line that emitted the trace) — clicking it expands/collapses the whole stack. The stack-header row itself is plain text now; the whole-row `.stack-header[data-gid]` click handler in `viewer-script-click-handlers.ts` still toggles the trace on row click, so accessibility and fallback interactivity hold when the counter column is hidden.

### Fixed
- **Async-suspension marker is now inline on the prior frame, click-to-expand, copy-safe** — the `⛓️‍💥` glyph that replaces Dart's `<asynchronous suspension>` rendered as its OWN row between every async frame, so an expanded Drift trace was still padded with N empty-looking rows whose only payload was the chain emoji. The glyph now folds onto the preceding frame's row instead — no standalone rows, the trace reads as a compact column of real frames with a small `⛓️` after each one. Clicking the glyph toggles a `.expanded` class that swaps the icon for the readable `<asynchronous suspension>` text inline, so the original phrase is one click away when it actually matters. The bare `)` that closes a `_StringStackTrace (#0 … )` envelope is dropped entirely — pure formatting noise with no payload to expand to, so it gets no row and no icon. Ctrl+C now produces the original `<asynchronous suspension>` text instead of the emoji: the icon comes from a CSS `::before` pseudo-element (which never enters clipboard text), and the verbatim phrase lives in a `.async-gap-text` span styled with the sr-only pattern (`position: absolute; clip: rect(0 0 0 0)`) so `getSelection().toString()` and `stripTags()` both still capture it. The prior frame's `rawText` is also extended with ` <asynchronous suspension>` so Alt+Shift+C (copyAsRawText) and the search index keep hitting the phrase verbatim.
- **Severity gutter connector redesigned — chain bugs structurally eliminated** — the line joining consecutive same-level dots was a JS chain machine: `renderViewport` walked siblings via `findNextDotSibling`, stamped `bar-up`/`bar-down`/`bar-bridge` and a second `level-bar-*` class onto neighbor rows, then CSS painted the stripe from those derived classes. With three layers (dot, stripe, bridge) all reading their own copy of the row's level, they could drift — and they did, producing the "blue line through green dots" symptom where the dot's `--bar-color` cascaded one way and the stripe's another. The whole chain machinery is removed; the connector is now declarative CSS using ten per-level `:has(+ .level-bar-X)::after` rules in `viewer-styles-decoration-bars.ts`. Each row paints its own stripe ONLY when its immediate next sibling shares the level — the browser does the sibling check natively. Same-level runs read as one continuous band; level transitions show as different colors meeting at the row boundary; lone rows show just the dot. The dot (`::before`) and the line (`::after`) read `--bar-color` from the same element via the same `level-bar-*` class, so they cannot disagree. ASCII-art rows excluded from the chain rule so their shimmer `::after` animation is preserved.
- **Activity-bar badge no longer accumulates while the Logs view is open** — the `(94)` count shown next to the **Saropa Log Capture** tab title is the native VS Code view badge for unread keyword-watcher hits. `updateWatchCounts()` was raising `unreadWatchHits` (and the badge) on every hit regardless of whether the view was visible, and `onDidChangeVisibility` only fires on visibility transitions — so hits landing while the panel was already open silently bumped the badge and it kept growing in front of the user. The host now checks `view.visible` before setting the badge: when any view is visible, `unreadWatchHits` is held at zero and the badge stays cleared; when the panel is hidden, the badge tracks the running total as before.
- **Signal panel no longer renders over the Logs/Sessions list with both visible at once** — `#panel-slot` stacks every slide-out panel in a single CSS-grid cell ([viewer-styles.ts:122-136](src/ui/viewer-styles/viewer-styles.ts#L122-L136)); the "only one visible at a time" invariant depended on `.visible` being mutually exclusive, which `setActivePanel()` enforces by calling `closeAllPanels()` first. Any code path that bypassed it — the host's `openSignalPanel` postMessage (used by "Open Full Signals" in the recurring panel, the Performance chip in the toolbar, and similar entry points) routed straight to `openSignalPanel()` which simply added `.visible` — could leave the previous panel (commonly Sessions/Logs) still `.visible`, so its content bled through the Signals panel as overlapping text. Added a `window.hideOtherPanelsInSlot(except)` helper in the icon-bar that strips `.visible` from every sibling panel; `openSignalPanel` and `openSessionPanel` now call it before adding their own `.visible` so the mutex holds regardless of caller.
- **Clicking a row in the Signals sidebar now opens the right session and scrolls to the line** — the cross-session signal trend rows posted only `signalType` (the kind: `error` / `warning` / `perf` / …), and the host resolver tried to find a session by looking up that kind in `signalSummary.counts` — but `SignalKind` values (`error`) never equal `SignalSummaryCounts` keys (`errors`), so the lookup never matched and clicks looked dead. Rows now also carry `data-fingerprint` and `data-detail` (raw example text), the click handler posts the fingerprint, and the host prefers a fingerprint match against `meta.fingerprints` / `warningFingerprints` / `perfFingerprints` / `driftSqlFingerprintSummary` / `signalSummary.entries` (V2). Fallback when no fingerprint match is found uses the typed lists, not the broken counts-key path. After the session loads (or immediately if it's already the active log), the host posts a new `scrollToSignal` message; the webview searches `allLines` for the raw example text — or for the longest non-placeholder segment of the normalized label — and scrolls + pulses that line.
- **Collapse-divider captions on by default, rendered as muted text** — when preview-mode stack collapse hid framework frames between two visible rows, the `.viewer-divider` row that announces the gap stayed in the DOM but its caption pill was suppressed (the accessibility setting `saropaLogCapture.accessibility.showCollapseDividerLabels` defaulted to off). The row's CSS still claimed `max(8px, 0.7em)` of height, so users saw an unexplained 8 px gap between e.g. line 177 and line 181 with no clue that frames 178-180 had been folded. Flipped the default to **on** so every collapsed gap reads as `─── N hidden lines · show ───`, and dimmed `.viewer-divider-label` to opacity `0.55` with a transparent background so the caption whispers instead of competing with real log lines. Hover lifts both the opacity to `1` and restores the badge fill so the click target is still obvious. The opt-out remains for users who prefer rows fully invisible at rest.
- **Scrollbar minimap palette synced with the per-row gutter** — the "rotate Info=blue / Notice=cyan / DB=green" palette change in commit `492d346f` updated `viewer-styles-decoration-bars.ts` but missed the canvas palette in `viewer-scrollbar-minimap-paint.ts`. The same source line painted a blue dot in the row gutter and a green tick on the scroll map (or any of the rotated pairs), so the minimap was unreliable as an overview of the gutter it sits next to. Synced the three rotated slots — info now `#2196f3` charts-blue, notice now `#00bcd4` terminal-cyan, database now `#4caf50` charts-green. Error / warning / performance / todo / debug were untouched by the rotation.

---

For older versions (7.11.2 and older), see [CHANGELOG_ARCHIVE.md](./CHANGELOG_ARCHIVE.md).
